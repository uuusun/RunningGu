# 런트립(RunTrip) — 최종 통합 명세서 (SPEC v2)

> **기준일 2026-07-16** · 이 문서가 프로젝트의 **단일 기준 명세(SSOT)** 다.
> 아래 4개 원천을 통합했으며, 내용이 충돌하면 **번호가 빠른 쪽이 우선**한다.
>
> | 우선 | 원천 | 처리 |
> |---|---|---|
> | ① | **팀 회의 결정 (2026-07-16)** — REQUIREMENTS.md를 함께 읽으며 확정한 최신 결정 9건 | 본 문서에 전부 반영 (부록 C에 결정↔반영 매핑) |
> | ② | `design/` 목업 **현행 코드** (2026-07-08 PR#6 머지 상태) | §3~§5·§8의 참조 구현 |
> | ③ | `REQUIREMENTS.md` (2026-07-08) — FE 행동 명세 | 본 문서로 **대체** |
> | ④ | 구 SPEC v1 (2026-07-02) — 서비스 정의·API 실측·데이터 계약 | 본 문서로 **대체** (원본: git `fc21c2e`) |
>
> ⚠️ REQUIREMENTS.md는 07-08 **머지 이전**(06-18 프로토타입) 코드 기준으로 작성되어 일부가 현행과 다르다
> (캘린더 뷰 "미구현"으로 기술, 러닝코스 "스텁"으로 기술, 포트 5180 등). 본 문서는 현행 코드 기준으로 바로잡았다.

**표기 규칙** (본 문서 전체 공통)

| 표기 | 의미 |
|---|---|
| ✅실측 | 2026-07-02 실제 API 호출로 확인 (부록 A) |
| 📄문서 | `docs/api/manuals/`·`Kakao/docs/api/manuals/` 매뉴얼 근거, 실호출 미수행 |
| 🔧정책 | 본 명세서가 정한 기본값 — 구현 중 조정 가능 |
| 🆕회의 | **2026-07-16 회의에서 신설·변경 확정** |
| 🧩목업 | `design/` 현행 구현이 곧 명세 (코드 = 참조 구현) |
| [미결-n] | §12의 n번 항목 — 다음 회의에서 확정 필요 |

---

## 0. 문서 이력과 지위

문서화 흐름: **SPEC v1 작성 → design/ 목업 제작(명세 대비 추가·삭제 발생) → 목업 기준으로 REQUIREMENTS.md 재작성 → REQUIREMENTS.md를 읽으며 회의(07-16) → 본 문서(v2)로 통합.**

| 자료 | 역할 |
|---|---|
| `SPEC.md` (이 문서, v2) | 최종 명세 SSOT |
| `design/` (Vite+React) | 확정 UX·화면 흐름·추천 엔진·러닝코스 빌더의 **참조 구현** |
| `data/races_sample.csv` → `design/public/data/races.json` | 대회 데이터 원천 (병합 153건) |
| `design/src/data/durunubi_courses.json` | 두루누비 걷기 코스 사전 파싱본 (**261개**, GPX 좌표 포함) |
| `submissions/…제안서_런닝구….pdf` | 기능 약속(M1~M4·차별성 4항)의 원천. 공모전 필수요건 "한국관광공사 OpenAPI 활용" 포함 |
| `docs/api/manuals/` · `Kakao/docs/api/manuals/` | KTO·카카오 API 파라미터 사전 |
| `web/` (Next.js) | 구 데모 — UX 기준 아님 (참고용) |
| `backend/` | 크롤 CSV→정규화 파이프라인 (§8.2에서 사용) |

---

## 1. 서비스 정의

### 1.1 개요

| 항목 | 내용 |
|---|---|
| 서비스명 | **런트립(RunTrip)** — 목업 워드마크·요구사항 명세의 확정 표기. ('런닝구(區)'는 **팀명·제안서 제출 명칭**으로 유지 — SPEC v1의 서비스명 표기를 정정) |
| 한 줄 정의 | 전국 마라톤 일정 통합 + 종목·체류 조건 맞춤 **대회 전후 여행 동선** 자동 추천 + 위치 기반 **러닝·산책 코스** + 러너 **커뮤니티**를 갖춘 러닝 관광 서비스 🆕회의 |
| 타깃 | 2030 러너 (5K·10K·하프 중심). 페르소나 "김러너"(28세 직장인, 10km 55분) |
| 부문 | 2026 관광데이터 활용 공모전 — 웹/앱 개발 (예비심사 통과) |
| 필수요건 | **한국관광공사 OpenAPI 활용 필수** (국문관광정보·두루누비·웰니스 3종이 핵심) |

**문제 정의.** ① 마라톤 일정이 블로그·인스타·개별 대회 홈페이지에 분산되어 신뢰할 수 있는 통합 서비스가 없다. ② 종목별 회복 강도와 동선을 반영한 대회 전후 여행 정보가 없다. ③ 지역의 "실제로 뛰기 좋은 코스" 정보는 현지 러너들 사이에만 있다. 🆕회의

**차별성 (제안서 4항 + 회의 확장 1항 — 모두 기능 요건).**

| # | 차별성 | 구현 위치 | 상태 |
|---|---|---|---|
| D1 | 대회→여행 원스톱 연결 (대회 선택 즉시 식당·관광지·축제를 지도 한 흐름으로) | 위저드 전체 + S7 결과 | 프로토타입 구현 |
| D2 | 자유 커스터마이징 동선 (추가·삭제·교체·순서변경) | `edits.js` + S7 편집 모드 | **구현 완료** |
| D3 | 러너 컨디션 기반 추천 룰 (하프/풀 D+1 고강도 제외, 5K/10K는 D-day 오후 관광) | `engine.js` + `RECOVERY` | **구현 완료** |
| D4 | 비(非)대회 시즌 커버 — 위치 기반 러닝·산책 코스 단독 탐색 | S8 러닝코스 (두루누비 빌더) | **구현 완료** 🧩목업 |
| D5 | 러너 커뮤니티 — 동선 공유 + 지역 러너가 직접 뛴 코스 공유 | S9 커뮤니티 (신설) | 미구현 🆕회의 |

### 1.2 기능 총괄 및 추적표

| 코드 | 기능 | 화면 | 데이터 소스 (§8) | 상태 |
|---|---|---|---|---|
| M1 | 종목·상황별 맞춤 동선 추천 | S4→S5→S6→S7 위저드 | KTO 위치기반 + 웰니스 + 카카오 로컬 하이브리드 | 엔진 완성 + 실데이터 153건. **산책 블록 제거 반영 필요(N-06)** 🆕회의 |
| M2 | 전국 마라톤 통합 캘린더 | S2 캘린더 | `races.json` 파이프라인 | **완료** (리스트+캘린더 뷰, 오늘 기준 접수상태) 🧩목업. **필터 모달 개편 필요(N-04)** 🆕회의 |
| M3 | 대회 인근 축제·행사 | S3 상세 + S1 홈 | `searchFestival2` 라이브(프록시) + 클라 거리계산 | **완료** (데모 기준) 🧩목업 — 배포 시 서버 프록시 필요 |
| M4 | 러닝·산책 코스 추천 | S8 러닝코스 | 두루누비 사전 파싱 261코스 + 카카오 걷기 스팟 | **완료** 🧩목업 |
| A1 | 동선 편집 (D2) | S7 편집 모드 + 후보 시트 | — | **완료** |
| A2 | 동선 저장·복원 | S10 보관함 | 로컬 상태 → **서버 영속화(N-07)** | 로컬 완료 → 보관함 개편 🆕회의 |
| A3 | 출처·최근확인일 표시 | S2 카드·S3 상세 | CSV `source`/`last_checked` | **완료** |
| A4 | 카톡 공유 (대회 카드·완성 동선) | S3·S7 공유 버튼 | Kakao Share SDK 📄문서 | 버튼만 존재 (G-09) |
| A5 | 이동시간 표시 | S7 타임라인 | 카카오모빌리티 ✅실측 | 미구현 (G-10) |
| A6 | 카카오내비 연발 | S7 | Kakao Navi SDK 📄문서 | P2 |
| A7 | 반려동물 동반 필터 | S5 옵션 (확장) | `KorPetTourService2` 📄문서 | P2 |
| A8 | 다국어 (인바운드) | 전체 | 웰니스 `langDivCd` 등 | 로드맵 4단계 |
| **U1** | 로그인 (이메일 + 카카오) | A1 | 백엔드 인증 API | 미구현 🆕회의 **P0** |
| **U2** | 회원가입 + 이메일 인증 + 약관 동의 | A2 | 백엔드 + 메일 발송 | 미구현 🆕회의 **P0** |
| **U3** | 비밀번호 찾기 (이메일 인증 → 임시 비밀번호 발급) | A3 | 백엔드 + 메일 발송 | 미구현 🆕회의 **P0** |
| **U4** | 구글·네이버 로그인 | A1·A2 | OAuth | **P2 — 시간 남으면** 🆕회의 |
| **H1** | 홈 화면 (검색 우선 + 기능 아이콘 + 마감임박 + 인기 대회 + 축제 추천) | S1 | races.json + 축제 API + 조회수 집계 | 미구현 🆕회의 **P0** |
| **F1** | 캘린더 필터 모달 (키로수·모집마감·지역) | S2 | — | 미구현 🆕회의 **P0** |
| **C1** | 커뮤니티 — 저장한 여행 동선 공유 | S9 | 백엔드 DB | 미구현 🆕회의 |
| **C2** | 커뮤니티 — 지역 기반 유저 러닝코스 공유 (두루누비 외 코스) | S9 | 백엔드 DB | 미구현 🆕회의 |
| **B1** | 보관함 (동선 + 내가 뛰거나 저장한 러닝코스) | S10 | 백엔드 DB | 미구현 🆕회의 **P0** |

---

## 2. 정보 구조(IA) · 내비게이션

### 2.1 하단 탭 5개 🆕회의

기존 4탭(캘린더·동선·러닝코스·내 여행)을 다음과 같이 개편한다.

| 탭 | 화면 | 비고 |
|---|---|---|
| **홈** | S1 | 신설 🆕회의 |
| **캘린더** | S2 (구 "홈/대회 캘린더") | 탭 라벨 변경은 회의 확정 — 최종 라벨 문구는 [미결-1] |
| **러닝코스** | S8 | 유지 |
| **커뮤니티** | S9 | 신설 🆕회의 |
| **보관함** | S10 (구 "내 여행") | 라벨·범위 변경 🆕회의 |

- **"동선" 탭 삭제** 🆕회의 — 동선 진입은 ① 캘린더에서 날짜/대회 선택 → 위저드, ② 보관함에서 저장 동선 열기, 두 경로로 일원화. 기존 동선 탭 빈 화면(`route`)도 함께 삭제.
- 탭바는 최상위 화면(S1·S2·S8·S9·S10)에서만 노출. 위저드·상세·결과(S3~S7)와 인증 화면(A1~A3)에서는 숨긴다. (S7 결과는 기존 "동선 탭 활성"에서 **탭바 숨김 화면**으로 변경 — 뒤로가기·저장으로 복귀 🔧정책)

### 2.2 화면 맵

| ID | 화면명 | 진입 경로 | 탭바 | 근거 |
|---|---|---|---|---|
| A1 | 로그인 | 앱 최초 실행 / 로그아웃 후 | 숨김 | 🆕회의 |
| A2 | 회원가입 | A1 "회원가입" | 숨김 | 🆕회의 |
| A3 | 비밀번호 찾기 | A1 "비밀번호 찾기" | 숨김 | 🆕회의 |
| S1 | 홈 | 로그인/게스트 진입 직후, 탭 [홈] | 표시 | 🆕회의 |
| S2 | 캘린더 (대회 리스트·캘린더) | 탭 [캘린더], S1 검색·아이콘 | 표시 | 구 S1 개편 |
| S3 | 대회 상세 | S2 대회 카드 / S1 대회 카드 | 숨김 | 유지 |
| S4 | 동선 만들기 — 일정 선택 | S3 CTA | 숨김 | 유지 (+직접 선택 🧩목업) |
| S5 | 동선 만들기 — 종목·취향 | S4 "다음" | 숨김 | 유지 |
| S6 | 동선 만들기 — 숙소 선택 | S5 "다음" | 숨김 | 유지 (카카오 API 확정 🆕회의) |
| S7 | 추천 동선 결과 | S6 추천 실행 / S10 동선 카드 | 숨김 🔧정책 | 산책 섹션 제거 🆕회의 |
| S8 | 러닝코스 (내 주변 / 지역별) | 탭 [러닝코스], S1 아이콘, S7 연계 카드 | 표시 | 🧩목업 |
| S9 | 커뮤니티 | 탭 [커뮤니티] | 표시 | 🆕회의 |
| S10 | 보관함 (동선 / 러닝코스) | 탭 [보관함] | 표시 | 🆕회의 |

### 2.3 주요 플로우

```
[인증]  A1 로그인 ── 회원가입 ──▶ A2 (동의 → 정보 입력 → 이메일 인증 → 완료) ──▶ A1
          │  └─ 비밀번호 찾기 ──▶ A3 (이메일 인증 → 임시 비밀번호 발송) ──▶ A1
          └─ 로그인 성공 / 카카오 로그인 / (게스트 둘러보기 [미결-6]) ──▶ S1 홈

[메인]  S1 홈 ──검색·달력 아이콘──▶ S2 캘린더 ──날짜·대회 선택──▶ S3 상세
          └─ 마감임박·인기 대회 카드 ─────────────────────────▶ S3 상세
        S3 ──"이 대회로 동선 만들기"──▶ S4 일정 ─▶ S5 종목·취향 ─▶ S6 숙소 ─▶ S7 결과
        S7 ──"이 동선 저장하기"──▶ S10 보관함[동선]
        S7 ──"숙소 주변에서 뛰기" 연계 카드──▶ S8 러닝코스(출발지=숙소) 🔧정책

[코스]  S8 내 주변: 위치/검색/프리셋 + 목표거리 ──▶ 두루누비 왕복 경로 + 걷기 스팟
        S8 ↔ S10 보관함[러닝코스] (코스 저장·뛴 기록)
        S9 커뮤니티: 동선·코스 공유 ↔ S10 보관함에 담기
```

### 2.4 상태 계약 (프론트 리듀서 — 목업 기준 + 개편분)

- 현행 🧩목업: `GO`(push) · `BACK`(pop) · `RESET_TO`(스택 초기화) · `TAB` · `SET`(부분 병합) · `SELECT_RACE`(대회 저장 + 기본 종목: 하프 보유 시 '하프', 아니면 첫 종목) · `BUILD_START`/`BUILD_DONE`(days/sources/recovery 저장) · `ACTIVE_DAY` · `TOGGLE_EDIT` · `OPEN_SHEET`/`CLOSE_SHEET` · `SET_DAYS`(편집 반영) · `SAVE_TRIP`(id 중복 제거 후 prepend) · `TOAST` · `RACES`(실데이터 로드).
- 개편 시 변경 🆕회의: `screen` 집합에서 `route` 제거, `auth(login/signup/reset)`·`community`·`archive` 추가. 저장 계열(`SAVE_TRIP` 등)은 로그인 사용자 기준 서버 동기화로 확장(§9.3).
- 대회 데이터: 앱 시작 시 `public/data/races.json`(153건) fetch → 실패 시 내장 샘플 폴백(NFR-1). 🧩목업

---

## 3. 공통 UI 규칙

### 3-1. 앱 프레임
- 모바일 1개 화면(폰 프레임, max-width 420px) 안에서 상태 기반 화면 전환(URL 라우팅 없음). 데스크톱에서는 중앙 목업 프레임.
- 상단 iOS 스타일 상태바(9:41)는 목업 장식.

### 3-2. 하단 탭바
- §2.1의 5탭. 현재 화면에 해당하는 탭을 활성 스타일로 표시. 탭 클릭 시 뒤로가기 스택을 초기화(RESET_TO)하고 해당 최상위 화면으로 이동.

### 3-3. 뒤로가기
- 화면 이동 시 이전 화면을 스택에 push, 뒤로가기(◀) 시 pop 복귀. 뒤로가기 시 편집 모드·후보 시트는 항상 해제. 스택이 비면 무시.

### 3-4. 토스트
- 화면 하단 성공형 토스트, **2.2초** 후 자동 소멸.
- 메시지: "내 여행에 저장했어요"→**"보관함에 저장했어요"** 🆕회의 · "동선을 공유했어요 (링크 복사)" · "동선을 내보냈어요 (이미지/링크)" · "동선을 못 불러왔어요. 다시 시도해 주세요" · 신규 🔧정책: "인증 메일을 보냈어요" · "가입이 완료됐어요" · "임시 비밀번호를 이메일로 보냈어요" · "커뮤니티에 공유했어요" · "보관함에 담았어요"

### 3-5. 로딩 / 빈 상태
- 비동기 조회 중 로딩 문구 + 버튼 비활성으로 중복 실행 방지: "숙소를 찾는 중…" · "동선 짜는 중…" · "불러오는 중…" · "축제를 찾는 중…" 🧩목업 · "위치를 확인하는 중…" 🧩목업
- 빈 상태 = 안내 제목 + 보조 문구 + 복구 액션 링크. (화면별 문구는 §4)

### 3-6. 소스 배지
- POI 목록·후보 시트에 데이터 출처 배지: `live`→**LIVE** / `sample`(사전수집)·`synth`(합성)→**샘플**. 폴백 투명성(NFR-2).

### 3-7. 카테고리 태그 / 회복 배지
- 카테고리 태그: 색상 + 한글 라벨 (관광지·맛집·카페·힐링웰니스·자연트레킹·역사문화·숙소·대회·회복 — "가벼운 산책"은 동선에서 제거 🆕회의).
- 회복 배지: 회복 모드(하프/풀)에만 표시. 라벨 "D+n 회복 모드"(D+ 없으면 "D-day 회복 모드") + 종목별 회복 안내 문구(§5.1).

### 3-8. 지도 (공통 컴포넌트 `MapView`) 🧩목업
- 카카오맵 SDK 준비 시 카카오맵, 아니면(키 없음/로드 실패, 4초 타임아웃) **SVG 폴백 지도**. 두 모드 인터페이스 동일.
- props: `pins[{n,id,lat,lng,title,catKey}]` · `polyline[[lat,lng]…]`(코스 경로선, 핀과 독립) · `connectPins`(기본 true — 핀을 잇는 동선 폴리라인) · `showLegend` · `activeId` · `onPinClick` · `accent` · `height`(기본 300).
- 핀: 방문 순서 번호 표시, 활성 핀 확대·강조. 핀 클릭 시 해당 블록/항목 활성화.
- 이동: 핀 구성 변경 시 전체 bounds 맞춤, 활성만 변경 시 panTo. `polyline` 변경 시 경로 전체 bounds.
- 범례: ● 동선(파랑) · ● 회복일(주황).

---

## 4. 화면별 상세 명세

> 각 기능은 **명세** 블록에 화면 표시 → 사용자 행동 → 시스템 동작 순으로 서술한다.
> 목업에 UI만 있고 동작이 없는 요소는 `[표시 전용]`으로 표기한다.

### 4.1 A1 로그인 🆕회의

**명세**
- 구성: 서비스 로고/워드마크 · 이메일 인풋 · 비밀번호 인풋 · [로그인] 버튼 · **[카카오로 시작하기]** 버튼 · 하단 링크 2개: "회원가입" · "비밀번호 찾기".
- (구글·네이버 로그인 버튼은 **P2 — 시간 남으면 추가** 🆕회의)
- [로그인]: 이메일/비밀번호 검증 → 성공 시 S1 홈으로 이동(스택 초기화). 실패 시 인라인 오류("이메일 또는 비밀번호를 확인해 주세요" 🔧정책).
- [카카오로 시작하기]: 카카오 OAuth 인가(§7.3) → 서버 토큰 교환 → 기존 연동 계정이면 로그인, 미가입이면 A2 카카오 가입 플로우로.
- 게스트(비로그인) 둘러보기 허용 여부·범위: [미결-6]. 제안 🔧정책: 탐색(S1·S2·S3·S8)은 게스트 허용, 저장·커뮤니티·보관함은 로그인 필수(진입 시 로그인 유도).

### 4.2 A2 회원가입 🆕회의

**명세** — 가입 흐름(회의 확정): **정보 입력(이메일·비밀번호·닉네임) → 이메일 인증 → 가입 완료**. 가입 시 **정보 동의 필수**.

1. **약관·개인정보 동의** — 필수 2종(서비스 이용약관 / 개인정보 수집·이용) + 선택(마케팅 수신) 🔧정책. 전체 동의 체크 제공. 필수 미동의 시 다음 단계 진행 불가. 동의 내역은 서버에 이력 저장(NFR-12).
2. **정보 입력** — 이메일(형식 검증 + 중복 확인) · 비밀번호(+확인 입력, 8자 이상 영문+숫자 조합 🔧정책) · 닉네임(2~12자, 중복 불가 🔧정책).
3. **이메일 인증** — [인증 메일 발송] → 6자리 코드 입력(유효 10분, 재발송 쿨다운 60초, 5회 실패 시 재발송 필요 🔧정책). 검증 성공 시에만 가입 확정.
4. **가입 완료** — 완료 화면/토스트 후 A1 로그인(또는 자동 로그인 🔧정책)으로.

- **카카오로 가입**: 카카오 OAuth → 프로필에서 닉네임 초기값 수신 → 약관 동의(1번과 동일) → 가입 완료. **이메일 인증 생략**(카카오 계정으로 신뢰 위임) 🔧정책.
- 구글·네이버 가입: P2 🆕회의.

### 4.3 A3 비밀번호 찾기 🆕회의

**명세** — 회의 확정: **이메일 인증으로 새 비밀번호를 생성해서 알려준다.**

1. 이메일 입력 → [인증 메일 발송]. (가입 여부는 노출하지 않고 "가입된 이메일이면 발송됩니다"로 안내 🔧정책 — 계정 존재 여부 탐지 방지)
2. 6자리 인증 코드 입력·검증 (A2와 동일 정책).
3. 검증 성공 시 서버가 **임시 비밀번호를 생성해 이메일로 발송**하고 안내 화면 표시.
4. 임시 비밀번호로 로그인 시 비밀번호 변경 유도(강제 여부 [미결-7]).

- 보안 노트: 임시 비밀번호도 서버에는 해시로만 저장(NFR-9). 재설정 링크 방식 대안 검토는 [미결-7].

### 4.4 S1 홈 🆕회의

**명세** — 회의 확정 컨셉: "**메인 기능을 처음에 보여주는 형식. 검색을 먼저 보여주고, 마라톤 검색 → 메인 기능으로 넘어가기.**"

구성 (위→아래):

1. **검색 바 (최상단)** — 플레이스홀더 "대회·지역 검색" 🔧정책. 검색 실행(입력 후 확정) 시 **S2 캘린더로 이동하며 검색어를 적용**한 결과 목록을 보여준다.
2. **기능 아이콘 행** — 바로가기 아이콘 4+개 🆕회의: **달력**(→S2 캘린더) · **지도**(→S8 러닝코스 '내 주변' 지도 중심) · **코스**(→S8 러닝코스) · **관광**(→ 홈 축제·관광 섹션 스크롤, 확장 시 전용 화면 — [미결-8]) 등.
3. **마감 임박 대회** — 접수중이면서 접수 마감일(`regEnd`)이 가까운 순 상위 6건 🔧정책. 가로 캐러셀 카드: 대회명 · 지역 · "마감 D-n" 배지. 카드 탭 → S3 상세.
4. **가장 많이 보는 대회 (인기)** — 조회수 상위 대회 🆕회의. 조회수는 서버 이벤트 집계(§9.3) — 집계 도입 전 MVP 산정 기준은 [미결-2]. 카드 탭 → S3 상세.
5. **축제·지역 관광 추천** — 진행 중·임박 축제 캐러셀. 데이터: `searchFestivalsNear` 어댑터 재사용(위치 권한 있으면 내 위치 기준, 없으면 전국 최신 🔧정책). 출처 "한국관광공사" 표기(NFR-7).

- 헤더: 로고 + 워드마크 "런트립". (구 홈의 우측 검색 아이콘 `[표시 전용]`은 검색 바 상시 노출로 대체 🆕회의)

### 4.5 S2 캘린더 (구 "홈 — 대회 캘린더")

**명세**

- **노출 대상**: 오늘 이후 대회만 (지난 대회 제외) 🧩목업. 접수 상태는 크롤 스냅샷이 아니라 **오늘 기준 재계산**(`regStatusOf`, §5.5) 🧩목업.
- **리스트/캘린더 뷰 토글** — 기본 리스트. 캘린더 뷰는 **구현 완료** 🧩목업 (REQUIREMENTS의 "미구현" 기술은 구식):
  - 월 네비(‹ YYYY.M ›) · 요일 헤더(일=빨강, 토=파랑) · 대회 있는 날 점 표시(2건 이상이면 건수 병기) · 오늘 날짜 테두리.
  - **날짜 탭 시 해당 일자의 대회 목록**을 아래에 표시(재탭 시 해제, 월 이동 시 선택 초기화). 미선택 시 이달 전체 목록. 🆕회의 "캘린더에서 날짜를 고르면 (대회→동선으로) 넘어가도록" — 날짜 → 대회 카드 → S3 → 동선 위저드가 동선 진입의 표준 경로.
  - 빈 상태: "이 날/달엔 대회가 없어요."
- **검색 인풋** — "대회·지역 검색", 입력 즉시 필터링 (대회명+장소+지역 부분 일치). S1에서 넘어온 검색어를 초기값으로 적용.
- **필터: 버튼 1개 + 모달** 🆕회의 — 기존 나열형 칩(접수 가능만·종목·지역·월)이 난잡하여 다음으로 교체:
  1. 검색 바 아래에 **[필터] 버튼 1개**. 그 옆에 **적용 중인 조건 칩**들을 나열(각 칩에 ✕ — 개별 해제).
  2. [필터] 탭 → **바텀시트 모달**:
     - **키로수(종목)**: 5K · 10K · 하프 · 풀 — 복수 선택 🔧정책 (UI 방식 상세 [미결-3])
     - **모집 마감 여부**: "접수 가능만" 토글 (기본 해제)
     - **지역**: 17개 시도 복수 선택 🔧정책
     - 하단 버튼: **[취소]**(변경 폐기하고 닫기 — 회의 확정) · **[완료]**(적용) · [초기화] 🔧정책
  3. [완료] 시 모달을 닫고 선택 조건을 필터 버튼 옆 칩으로 나열.
  - **월 칩은 제거** — 월 탐색은 캘린더 뷰의 월 네비가 담당 🆕회의(모달 구성에 월 없음).
  - 모든 조건은 AND 결합: 종목 ∧ 접수 가능 ∧ 지역 ∧ 검색어.
- **대회 목록** — 결과 건수 "대회 N", 개최일 오름차순. 0건 시 빈 상태("조건에 맞는 대회가 없어요." / "필터를 바꿔보세요." / [필터 초기화]).
- **대회 카드** — 날짜 컬럼(영문 월/일/요일) · 대회명 + 하트 `[표시 전용]`([미결-9]) · 지역·장소 · 종목 태그(첫 태그 강조) · 접수 상태 칩(접수중 open / 그 외 closed+카드 흐림) · **출처 "{source} · 확인 MM.DD"**(A3 요건).
  - featured 카드: 목록 첫 번째 + 접수중 → 강조 스타일.
  - 카드 탭 → 대회 전역 선택(기본 종목: 하프 보유 시 하프) → S3 이동.

### 4.6 S3 대회 상세

**명세**

- 앱바: 뒤로가기 · 공유 `[표시 전용]`(G-09) · 저장(하트) `[표시 전용]`([미결-9]).
- 히어로: "{지역} · 마라톤" + 대회명 · **D-day**(오늘 기준 실계산 — 하드코딩 제거 완료 🧩목업) · 개최일 "MM.DD 요일" · "출발 {시각} · {장소}" · 접수 상태 배지(접수중: 라임 강조 + "~MM.DD").
- 정보 행: 종목(" · " 연결) · 접수 기간 · 주최 · "출처 {source} · 확인 MM.DD" + [공식 페이지 ↗](새 탭).
- **대회 기간 인근 축제 (M3)** — **실연동 완료** 🧩목업 (REQUIREMENTS의 "정적 샘플 2건" 기술은 구식):
  - 섹션 타이틀 "대회 기간 인근 축제" + 우측 출처 "한국관광공사".
  - 데이터: `searchFestivalsNear` — KTO `searchFestival2`를 **대회일 ±14일** 윈도우로 조회 → 클라 Haversine으로 **대회장 반경 40km** 필터 → 거리순 **최대 6건** 🧩목업🔧정책.
  - 카드: 축제 이미지(`firstimage`) · 축제명 · "MM.DD ~ MM.DD · 대회장 {d.d}km".
  - 상태: 로딩 "축제를 찾는 중…" / 빈 상태 "대회 기간에 열리는 인근 축제가 없어요." + "대회 전후 여행 동선은 그대로 짜드릴 수 있어요."
- CTA(하단 고정): **"이 대회로 동선 만들기"** → S4.

### 4.7 S4 동선 만들기 — 일정 선택

**명세**

- 앱바 "동선 만들기" · 타이틀 "언제 다녀올까요?" · 보조 "{대회명} (MM.DD) 기준으로 짜드릴게요."
- **일정 패턴** — 단일 선택, 기본 "전후로":

  | 패턴 | 부제 | 범위(대회일 기준) |
  |---|---|---|
  | 전날부터 | 1박 2일 | D-1 ~ D-day |
  | 대회+다음날 | 1박 2일 | D-day ~ D+1 |
  | 전후로 | 2박 3일 | D-1 ~ D+1 |
  | 당일치기 | 당일 | D-day |
  | **직접 선택** 🧩목업 | 날짜를 눌러 지정 | 캘린더에서 시작·종료 탭 |

- **직접 선택** 동작 🧩목업: 미니 캘린더가 탭 가능 상태가 되고, 첫 탭 = 시작일(힌트 "종료일을 눌러주세요"), 둘째 탭 = 종료일. 시작보다 앞 날짜를 두 번째로 탭하면 자동으로 시작/종료를 정렬.
- 패턴 선택(및 진입) 시 pattern/start/end를 전역 상태에 즉시 동기화.
- **미니 캘린더**: 대회 개최 월 표시. 대회일 = primary 배경, 여행 기간 = soft 하이라이트. 범례(■ 대회일 · ■ 여행 기간) + 힌트 "MM.DD ~ MM.DD · n일".
- CTA "다음" → S5.

### 4.8 S5 동선 만들기 — 종목·취향

**명세**

- 타이틀 "어떻게 즐기고 싶으세요?"
- **종목 (단일)** — 라벨 "종목 · 회복강도 {intensity}"(선택 종목의 강도 실시간 반영). 세그먼트 4종 **5K | 10K | 하프 | 풀** 항상 노출(대회에 없는 종목도 선택 가능).
  - 기본값: 이전 선택 → 대회 종목 중 하프 우선 → 첫 종목.
  - 힌트: 대회 종목 포함 시 "대회에서 가져왔어요 · 바꿀 수 있어요" / 미포함 시 "이 대회 종목엔 없지만 선택할 수 있어요".
- **여행 취향 (복수)** — 칩 6종: 관광지 · 맛집 · 카페 · 힐링·웰니스 · 자연·트레킹 · 역사·문화. 기본 [관광지, 맛집]. 0개면 CTA 비활성.
- **회복 안내** — 하프/풀 선택 시에만: "{종목}는 완주 다음날 회복이 중요해요. **D+1은 고강도 일정을 빼고** 온천·가벼운 산책 위주로 동선을 짭니다."
- CTA "다음" → 종목·취향 저장 후 S6.

### 4.9 S6 동선 만들기 — 숙소 선택

**명세**

- 타이틀 "어디서 묵을까요?" · 보조 "건너뛰면 대회장 주변으로 잡아드려요." 🧩목업 (문구 간소화 반영)
- 숙소 선택은 **선택 사항**.
- **숙소 데이터는 카카오 API로 가져온다** 🆕회의(확정) — 현행 구현과 일치: 카카오 로컬 **AD5(숙박)** 카테고리 검색, 대회장 좌표 중심 최대 8건, 거리순. 폴백 체인(§8.1: live→sample→synth) + 섹션 라벨 "대회장 주변 추천" 옆 소스 배지.
- 검색 인풋 "숙소 검색 · 카카오 로컬" — 추천 목록 로컬 필터(숙소명+주소 부분 일치).
- 항목: 썸네일 · 숙소명 · "{주소} · {설명}" · [선택](재선택 시 교체, "선택됨" 표시).
- CTA 라벨 전환: 선택 시 "이 숙소로 동선 추천받기" / 미선택 "숙소 없이 추천받기" / 생성 중 "동선 짜는 중…"+비활성.
- 실행: 숙소(또는 null) 저장 → 추천 엔진(§5.6) 실행 → 성공 시 S7(첫 일자 활성), 실패 시 빈 동선으로 S7 진입 + 실패 토스트.

### 4.10 S7 추천 동선 결과

**명세**

- **상단 지도(높이 300)** — §3-8 준수. 활성 일자의 좌표 보유 블록만 순서 번호 핀 + 폴리라인. 회복일(D+)은 주황, 그 외 파랑. 오버레이: 좌상단 뒤로가기 · 우상단 공유(토스트 "동선을 공유했어요 (링크 복사)" `[MVP 목업 동작]`).
- **요약/일자 탭** — 회복 배지(조건부) · "{지역} {당일치기|n박 n+1일}" + "{전체 장소 수}곳" · 일자 탭("D-1 · 06.17 수", 회복일은 회복 스타일). 탭 클릭 → 일자 활성화 + 지도 재계산 + 첫 핀 활성. 일자 라벨 줄 + [편집] · 일자 노트(엔진 생성 안내 문구).
- **타임라인(조회 모드)** — 시간순 카드: 좌측 레일(순서 번호 + 세로 라인) · 제목+시간 · 카테고리 태그(대회·숙소 블록은 생략)+장소명 · 설명(장소명과 같으면 생략). 활성 카드 강조.
- **지도 ↔ 타임라인 동기화** — 카드 클릭→핀 이동 / 핀 클릭→카드 중앙 스크롤 / 스크롤 중앙 밴드(상하 45% 제외) 자동 활성화. 편집 모드에서는 스크롤 동기화 중단.
- **편집 모드** — [편집]↔[완료]. 안내 "드래그로 순서 변경 · ↺ 교체 · ✕ 삭제". 행: ☰ 그립 + 번호 + 제목 + "{시간}·{장소}·{카테고리}" + 조작 버튼.
  - 순서 변경(같은 일자 내 드래그) · 교체(↺ — 후보 시트 '교체' 모드, **대회 블록(🏁)은 교체 불가**) · 삭제(✕ 즉시) · 추가([+ 장소 추가] — 후보 시트 '추가' 모드, 기본 카테고리 관광지).
  - 결과는 즉시 반영, 지도 핀·폴리라인·장소 수 자동 재계산 (불변 조작, §5.7).
- **후보 시트 (교체/추가 공용 바텀시트)** — 스크림 클릭 시 닫힘. 타이틀 "{카테고리} {교체|추가} · 인근" + 소스 배지. 추가 모드에서만 카테고리 칩(취향 6종 + 숙소) 노출·재조회. 장소 검색 줄 `[표시 전용]`. POI 최대 8건(중심: 숙소 있으면 숙소, 없으면 대회장). [선택]: 교체=장소·설명·카테고리 교체(블록 id·시간 유지) / 추가=새 블록(13:00)을 일자 맨 끝에 추가 → 시트 닫기.
- ~~숙소 주변 산책 코스 섹션~~ — **삭제** 🆕회의. 숙소 주변 산책·러닝 코스는 S8 러닝코스 기능이 담당한다(회의 사유: "러닝 코스 짜는 기능에서 나타내면 되기 때문").
  - 대체 🔧정책: 조회 모드 하단에 **연계 카드 1개** — "숙소 주변에서 뛰기·걷기 → 러닝코스에서 보기". 탭 시 S8 '내 주변'으로 이동하며 출발지=숙소(없으면 대회장) 좌표, 목표 거리 기본값=min(RECOVERY[종목].walk, 5)km를 전달.
- **저장/내보내기 CTA** — 내보내기(고스트): 토스트 `[MVP 목업 동작]`. **"이 동선 저장하기"**: 여행 객체(id=`{대회id}-{시작일}-{종료일}`) 구성 → **보관함[동선]** 맨 앞 저장(동일 id 교체 — 중복 방지) → 토스트 "보관함에 저장했어요" 🆕회의. 로그인 사용자는 서버 영속화(N-07).
- **빈 상태** — "동선이 아직 없어요." + [다시 추천받기](엔진 재실행).

### 4.11 S8 러닝코스 — **구현 완료** 🧩목업 (REQUIREMENTS의 "MVP 스텁" 기술은 구식)

**명세** — 타이틀 "러닝·산책 코스". 상단 토글: **내 주변 | 지역별**.

**(a) 내 주변 — 위치 기반 코스 빌더**

1. **출발지 정하기** — 3가지 방법:
   - GPS "내 위치" (권한 요청, 타임아웃 6초)
   - **출발지 검색** (카카오 로컬 키워드 지오코딩, 예: "경주역, 해운대" — 첫 결과 자동 적용)
   - 프리셋 칩 5종(부산 해운대·여수·강릉·인천 강화·서울시청 — GPS 대체/데모용)
   - S7 연계 카드로 진입 시 출발지=숙소 좌표 프리필 🔧정책.
2. **목표 거리 슬라이더** — "몇 km 뛸까요" **1~21km, 0.5km 단위**, 기본 5km. (범례 "1km / 21km (풀코스 하프)")
3. **경로 생성 규칙(`buildRouteNear`)** — §5.8. 출발지 반경 8km 내 두루누비 코스의 최근접 진입점을 찾아, 목표 거리의 절반만큼 더 길게 뻗는 방향으로 편도 구간을 뽑고 **왕복 경로**(출발점 복귀)로 구성. 진입점 거리순 최대 12건.
4. **지도(높이 230)** — 선택 코스의 왕복 경로를 폴리라인으로 표시(경로 전체 bounds). 걷기 스팟은 번호 핀(선으로 잇지 않음, 리스트 번호와 일치).
5. **선택 코스 요약 카드** — "{코스명} 왕복" · "{시군} · 출발지→진입점 {m|km}" · 태그: {왕복 km} · 약 {분}분(분당 110m 가정) · 난이도. 코스가 목표보다 짧으면 "이 코스가 짧아 목표({n}km)보다 짧게 짜였어요."
6. **대체 코스 목록** — "다른 코스 N개", 탭 시 선택 교체.
7. **걷기 좋은 곳 보강 (도시·두루누비 공백 지역)** — 카카오 로컬 키워드(공원·산책로·둘레길·하천) 반경 3km, 걷기 적합 카테고리만 필터(§5.9), 거리순 최대 12곳. 항목: 번호 · 장소명 · "{카테고리} · {거리}" · 주소 (탭 시 카카오 장소 페이지 새 탭). 섹션 라벨 "이 근처 걷기 좋은 곳 · 카카오".
   - 두루누비 코스 0건 + 스팟 있음 → 인포박스 "이 근처엔 두루누비 코스가 없어요. 대신 걸을 만한 공원·하천을 모았어요."
   - 둘 다 0건 → 빈 상태 "이 근처엔 걸을 곳을 못 찾았어요." + 프리셋 안내.
   - 위치 미정 → "위치를 확인하는 중… / 출발지를 정해주세요."

**(b) 지역별 — 두루누비 브라우징**

- 시도 칩(코스 수 내림차순 정렬), 단일 선택·재탭 해제.
- 목록: "{지역|전국} 코스 N · 두루누비 걷기길", 거리 오름차순. 카드: 코스명 · "{시군} · {km} · {난이도} · 약 {분}분". 빈 상태 "이 지역엔 코스가 없어요."

**(c) 확장** — 커뮤니티 코스(두루누비 외 유저 공유 코스)를 '내 주변' 결과에 병합 노출 🆕회의 연계, P2 🔧정책. 코스 저장 → S10 보관함[러닝코스].

### 4.12 S9 커뮤니티 🆕회의 (신설 — 서버 필수)

**명세** — 회의 확정 목적: ① 개인이 저장한 **여행 동선 공유**, ② **두루누비 이외의 러닝 코스 추천**, ③ **지역을 설정하면 동네 주민·여행객이 직접 뛴 코스를 공유받고 이용**.

- **피드**
  - 상단: 타입 세그먼트 [전체 | 동선 | 러닝코스] + **지역 선택 칩**(17개 시도 — "지역을 설정하면" 요건).
  - 게시물 카드:
    - 동선 공유: "{지역} {n박 n일}" · 대회명·종목 · 기간 · 장소 수 · 작성자 닉네임.
    - 코스 공유: 지도 미리보기(경로 폴리라인) · 코스명 · "{지역} · {km} · {소요}" · 작성자 닉네임.
  - 정렬 기본 최신순 🔧정책 (인기순 등은 [미결-4]).
- **상세**
  - 동선: 읽기 전용 타임라인 + 지도(S7 조회 모드 재사용). CTA **[보관함에 담기]** → 내 보관함[동선]에 사본 저장(토스트 "보관함에 담았어요").
  - 코스: 지도 경로 + 거리·난이도·설명. CTA **[이 코스 저장]** → 보관함[러닝코스]. (러닝코스 '내 주변'에서 재사용 가능)
- **등록**
  - 동선 공유: S10 보관함[동선] 카드의 [커뮤니티에 공유] → 제목·한 줄 소개 입력 → 게시(동선 스냅샷 저장).
  - 코스 공유: 등록 방식(두루누비 슬라이스 공유 / 지도에서 경유점 그리기 / GPX 업로드 / 뛴 기록)은 **[미결-4]** — MVP 제안 🔧정책: S8에서 만든 왕복 경로 공유 + 지도 탭 경유점 그리기.
- 좋아요·댓글·신고: 포함 여부 [미결-4]. 최소 운영 장치로 신고·관리자 삭제는 권장(NFR-13).
- 게스트는 읽기 전용 🔧정책([미결-6]과 연동).

### 4.13 S10 보관함 (구 "내 여행") 🆕회의

**명세** — 회의 확정: 탭 이름을 '내 여행'→**'보관함'**으로 바꾸고, **대회 동선 짠 것 + 내가 뛰거나 저장한 러닝코스**를 함께 보관한다.

- 상단 세그먼트: **[동선] | [러닝코스]**.
- **[동선] 탭** (구 내 여행 승계)
  - 카드: "{지역} {당일치기|n박 n일}" · "{대회명} · {종목}" · 회복 배지(회복 모드만) · "MM.DD ~ MM.DD · {장소 수}곳".
  - 카드 탭 → 저장 시점 상태(대회·종목·취향·기간·days·회복) 복원 → S7 결과로 이동(활성 일자 첫날).
  - 카드 액션: [커뮤니티에 공유] 🆕회의 · [삭제] 🔧정책.
  - 빈 상태: "아직 저장한 동선이 없어요." · "대회를 골라 첫 동선을 만들어 보세요." · [대회 둘러보기]→S2.
- **[러닝코스] 탭** 🆕회의
  - 항목 종류: `saved`(S8/커뮤니티에서 저장한 코스) · `ran`(**내가 뛴 코스** — 완료 표시). '뛰었어요' 판정은 MVP에서 수동 완료 체크 🔧정책, GPS 자동 기록은 [미결-5].
  - 카드: 코스명 · "{지역} · {km} · {난이도}" · (ran이면 완료 배지 + 뛴 날짜). 탭 → 지도 경로 상세(S8 재사용).
  - 빈 상태: "저장한 코스가 없어요." + [러닝코스 둘러보기]→S8.
- 저장 데이터는 로그인 계정에 **서버 영속화**(N-07 — 기존 G-14 localStorage 계획을 대체·승격).

---

## 5. 도메인 규칙

### 5.1 종목별 회복 룰 `RECOVERY` (constants.js — verbatim)

| 종목 | walk(km 상한) | noHard | intensity | dday | dplus |
|---|---|---|---|---|---|
| 5K | 8 | false | 거의 정상 | 완주 후 오후부터 자유 관광 | 일반 관광 자유 |
| 10K | 8 | false | 낮은 피로 | 완주 후 가벼운 관광·축제 | 일반 관광 |
| 하프 | 5 | **true** | 중등도 피로 | 완주 후 온천·휴식 권장 | 온천+짧은 산책(고강도 제외) |
| 풀 | 3 | **true** | 고강도 회복 필요 | 완주 후 회복 집중, 도보 최소 | 스파·온천 중심, 도보 최소 |

- `walk` 상한의 용도 변경 🆕회의 반영: 동선 내 산책 블록이 사라지므로, S7→S8 연계 시 **러닝코스 목표 거리 기본값**(min(walk, 5)km)으로 재사용 🔧정책.

### 5.2 일정 패턴 `PATTERNS`

| 키 | 라벨 | 부제 | 오프셋(대회일 기준) |
|---|---|---|---|
| pre | 전날부터 | 1박 2일 | [-1, 0] |
| post | 대회+다음날 | 1박 2일 | [0, +1] |
| around | 전후로 | 2박 3일 | [-1, +1] |
| day | 당일치기 | 당일 | [0, 0] |
| custom 🧩목업 | 직접 선택 | 날짜를 눌러 지정 | 사용자 지정 [start, end] |

### 5.3 취향 카테고리 `CATS` / 숙소 `LODGING_CAT`

| 키 | 라벨 | POI 조회 방식 (카카오 로컬) |
|---|---|---|
| tour | 관광지 | 카테고리 AT4 |
| food | 맛집 | 카테고리 FD6 |
| cafe | 카페 | 카테고리 CE7 |
| wellness | 힐링·웰니스 | 키워드 "온천 스파 사우나 찜질방" |
| nature | 자연·트레킹 | 키워드 "둘레길 공원 산책로 수목원" |
| history | 역사·문화 | 키워드 "박물관 유적지 문화재" |
| lodging | 숙소 (칩 미노출·검색 전용) | 카테고리 AD5 (+키워드 폴백 "호텔 모텔 게스트하우스 펜션") 🆕회의 확정 |

### 5.4 종목 표준화 (events.js) 🧩목업

- `stdEvent()`: `풀|full|42→'풀'` · `하프|half|21→'하프'` · `10k|10km|단독 10→'10K'` · 그 외 `'5K'`. 중복 제거, 순서 `['풀','하프','10K','5K']`.
- `stdEventKm()` (거리→종목 버킷): **≥32→풀 · ≥18→하프 · ≥9→10K · 그 외→5K** (트레일·비표준 거리 11/15/28/40km 흡수).
- `eventsFromRace()` 우선순위: ① `has_*` 플래그 → ② `distances` 거리 버킷 → ③ `event_types` 토큰.

### 5.5 접수 상태 규칙 `regStatusOf` 🧩목업

크롤 시점 스냅샷(`reg_status`)은 stale하므로 **오늘 기준으로 재계산**한다:
`regEnd < 오늘 → 마감` / `오늘 < regStart → 접수전` / `그 사이 → 접수중` / 기간 정보 없으면 원본 `regStatus`(없으면 '미정').

### 5.6 동선 생성 엔진 `buildItinerary` — v2 (산책 블록 제거 반영 🆕회의)

엔트리: `buildItinerary({ race, stay, event, themes, start, end })` → `{ days, sources, recovery }`. **UI·지도 비의존 순수 모듈**.

1. 선택 종목의 회복 룰 결정(§5.1).
2. 카테고리 풀: `{food, tour} ∪ themes` — **noHard면 wellness 추가, 아니면 cafe 추가.**
3. POI 적재: 각 카테고리 대회장 중심 최대 8건(§8.1 폴백 체인). ~~산책 풀(숙소 중심 nature 6건)~~ — **제거** 🆕회의.
4. 일자별 블록 생성 (장소는 전체 일정에서 **중복 없이** 배정 — used Set):

   | 일자 | 블록 (🆕회의: 산책 블록 3종 제거 후) | 노트 |
   |---|---|---|
   | **D-1** | 15:00 숙소 체크인(lodging) → 18:30 카보로딩 저녁(food) → ~~20:00 가벼운 저녁 산책~~ | "내일 완주 · 가볍게 먹고 푹 쉬기" |
   | **D-day (회복 ○)** | {출발시각} 🏁 대회 스타트(race) → 11:00 온천·회복(wellness) → [하프만] 14:30 가벼운 관광(tour) → 18:00 회복 저녁(food) → ~~20:30 숙소 주변 저녁 산책~~ | rule.dday |
   | **D-day (회복 ✕)** | {출발시각} 🏁 스타트 → 13:00 오후 자유 관광(테마 우선) → 15:30 카페(cafe) → 18:30 맛집 저녁(food) → ~~20:30 산책~~ | rule.dday |
   | **D+N (회복 ○)** | ~~08:00 아침 산책~~ → 10:00 온천·족욕(wellness) → 12:30 로컬 점심(food) → 14:30 오후 관광(테마 우선) → [마지막날] 17:00 체크아웃·귀가(lodging) | rule.dplus |
   | **D+N (회복 ✕)** | ~~08:00 아침 산책~~ → 10:00 오전 관광(tour) → 12:30 로컬 점심 → 14:30 오후 관광(테마 우선) → [마지막날] 17:00 체크아웃·귀가 | rule.dplus |

   - 산책 수요는 S7 하단 연계 카드 → S8 러닝코스로 이관(§4.10) — 회의 사유 반영.
   - 부수 효과: `walkDesc` 무효 코드 이슈(구 G-13)는 walk 블록 제거로 함께 소멸.
5. **테마 우선 선택**: `[...themes, 'tour', 'nature', 'cafe', 'history']` 순서로 미사용 POI가 있는 첫 카테고리.
6. **회복 배지**: noHard 아니면 null. D+ 존재 시 `"D+n 회복 모드"`, 없으면 `"D-day 회복 모드"` + rule 문구.
7. 결과 `days[]`는 **편집 가능한 초안** — §5.7 편집 연산으로 수정.

### 5.7 편집 연산 (edits.js)

모든 연산은 days/blocks를 불변(immutable) 조작하며 지도 핀·폴리라인·장소 수는 파생값으로 자동 재계산된다.

| 연산 | 동작 |
|---|---|
| updateBlock | 블록 필드(시간·제목·설명 등) 수정 |
| removeBlock | 블록 삭제 |
| replacePlace | 장소·설명·카테고리 교체 (블록 id 유지) |
| addBlock | 새 블록 추가 (맨 끝/지정 위치, 새 id) |
| moveBlock | 같은 일자 안 순서 이동 |
| dayPins / countPlaces | 파생값: 일자별 핀 목록 / 전체 장소 수 |

### 5.8 러닝코스 빌더 규칙 (courses.js) 🧩목업

- 원천: `durunubi_courses.json` — 두루누비 걷기길 **261개** 코스 사전 파싱본. 코스당 `{id, name, distKm, level(1~3), minutes, cycle, sigun, sido, summary, points[[lat,lng]…]}` (GPX에서 추출한 트랙포인트).
- `browseCourses({region, minKm, maxKm, level})` — 시도·거리·난이도 필터, 거리 오름차순.
- `buildRouteNear({lat, lng, targetKm, radiusKm=8, limit=12})` — **왕복 경로 생성**:
  1. 각 코스에서 출발지 최근접 포인트(진입점) 탐색, 진입점까지 radiusKm 초과 시 제외.
  2. 진입점에서 앞/뒤 두 방향으로 `targetKm/2` 편도 구간을 뽑아 **더 길게 뻗는 방향** 선택.
  3. 편도 + 역방향 복귀를 이어 왕복 `routePoints` 구성. `routeKm`(왕복 실거리) · `minutes`(분당 110m 가정) · `accessM`(진입점까지 거리) · `shortfall`(왕복이 목표-300m 미만이면 true).
  4. `accessM` 오름차순 limit건.
- `sliceCoursesNear` — 동일 탐색으로 편도 구간만 추출(리스트용, 왕복 아님).
- `courseRegions()` — 시도별 코스 수 내림차순.

### 5.9 걷기 스팟 필터 (tourapi.js `searchWalkSpots`) 🧩목업

- 카카오 로컬 키워드 4종(공원·산책로·둘레길·하천) × 반경 3km(size 15, 거리순) 조회 후:
  - **포함**: category_name이 `공원|관광|명소|산책|둘레|하천|유원지|수목원|숲|생태|휴양|호수|해수욕|해변|등산로|트레킹|자연` 매칭.
  - **제외**: 장소명이 `화장실|주차장|주차|테니스|풋살|축구장|야구장|농구장|체육관|관리사무소|매점|안내소|정류장` 매칭 (하위 시설 제거).
- 이름+주소로 중복 제거, 거리순 최대 12곳.

---

## 6. 데이터 명세

### 6.1 대회 원천과 표준 Race 계약 (SSOT — `design/src/lib/runninggu/normalize.js`)

- 원천: `data/races_sample.csv` **271행**(RFC 4180 파서 필수 — 멀티라인 description) → 파이프라인 병합 **153개 고유 대회** → `design/public/data/races.json`.
- 33컬럼 스키마·수집 통계·품질 이슈는 SPEC v1 §4.1과 동일 (출처: 마라톤온라인 138 + 마라톤GO 133, 좌표 100%, 대회일 2026-06-06~11-15).

```
Race {
  id, name, region, venue, date,          // date = 'YYYY-MM-DD' (event_date)
  startTime,                              // ← start_time ('HH:MM')
  eventTypes,                             // ← has_* 플래그/거리 → ['풀','하프','10K','5K'] 표준화 (§5.4)
  regStatus, regStart, regEnd,            // 표시용 상태는 regStatusOf()로 오늘 기준 재계산 (§5.5)
  organizer, source, checked,             // 출처·최근확인일 배지 (A3)
  officialUrl, detailUrl, imageUrl,
  lat, lng,                               // Number 강제
  category                                // 로드|트레일|걷기|야간
}
```

- `region`(표시용)과 별개로 필터용 **regionCode 17종**(서울·부산·대구·인천·광주·대전·울산·세종·경기·강원·충북·충남·전북·전남·경북·경남·제주)을 `sido`에서 파생. `'충청'` 등 비표준 값은 `road_address`/`venue`로 보정 🔧정책.

### 6.2 POI 계약 (모든 소스 공통)

```
Poi { name, lat, lng, desc, addr, url }
```

- 카카오 변환: `place_name→name, Number(y)→lat, Number(x)→lng, category_name 말단→desc, road_address_name||address_name→addr, place_url→url`.
- KTO 변환 🔧정책: `title→name, mapy→lat, mapx→lng, 분류 라벨→desc, addr1→addr` + 확장 `image(firstimage), contentId, dist`.
- 조회 결과는 `{ source: 'live'|'sample'|'synth', places: Poi[] }` — 폴백 체인 §8.1. 소스는 UI 배지 노출(NFR-2).

### 6.3 일정·저장 계약

```
day    { date, off, label('D-1'|'D-day'|'D+n'), dateLabel, note, blocks }
block  { id('blk_N', 편집 간 유지), time, title, catKey, place: Poi|null, desc }
trip   { id: '{raceId}-{start}-{end}', race, raceName, region, event, themes,
         start, end, days, recovery, userId? }        // userId: 서버 영속화 시 🆕
```

### 6.4 러닝코스 계약 🧩목업 + 🆕회의 확장

```
course       { id, source: 'durunubi'|'user',            // user = 커뮤니티 공유 코스 🆕
               name, sido, sigun, distKm, level(1|2|3), minutes, cycle?,
               summary?, points[[lat,lng]…], createdBy?(userId) }
builtRoute   { id, parentName, sido, sigun, levelLabel, fullDistKm,
               accessM, routeKm, minutes, shortfall, start, routePoints }   // buildRouteNear 산출
walkSpot     { name, category, addr, lat, lng, distM, url }                 // 카카오 걷기 스팟
savedCourse  { userId, kind: 'saved'|'ran', course|builtRoute 스냅샷, savedAt, ranAt? }  // 보관함 🆕
```

### 6.5 회원·커뮤니티 계약 🆕회의 (초안 — 백엔드 설계 시 확정)

```
user          { id, email, nickname, provider: 'email'|'kakao'('google'|'naver' P2),
                emailVerified: bool, passwordHash?,         // 소셜 가입은 null
                agreements: { tos: at, privacy: at, marketing?: at },   // 동의 이력 (필수)
                createdAt }
emailAuth     { email, code(6자리), purpose: 'signup'|'reset', expiresAt(+10분), attempts, verified }
communityPost { id, type: 'trip'|'course', authorId, region(시도), title, body?,
                payload: trip 스냅샷 | course, views, likes?, createdAt }
raceView      { raceId, count }                              // 인기 대회 집계용 (H1)
```

### 6.6 좌표·날짜 규칙 (버그 1순위 — 전 계층 준수)

| 컨텍스트 | 규칙 |
|---|---|
| 좌표계 | 전부 **WGS84** (KTO도 WGS84 — 📄문서 + ✅실측 일치) |
| 카카오 REST·KTO 요청/응답 | **x = 경도(lng), y = 위도(lat)** / KTO는 `mapX`(경도)·`mapY`(위도) |
| 카카오맵 JS SDK | `kakao.maps.LatLng(위도, 경도)` — **REST와 인자 순서 반대** |
| KTO 응답 필드명 | KorService2는 소문자 `mapx/mapy`, 웰니스는 **대문자 `mapX/mapY`** 📄문서 |
| 내부 표준 | `lat`/`lng` 숫자 필드 — 경계에서만 변환, 내부는 lat/lng만 사용 |
| 날짜 | `'YYYY-MM-DD'` 로컬 자정 기준(dates.js). KTO 날짜 파라미터는 `YYYYMMDD` |
| 두루누비 points | `[[lat, lng], …]` 배열 (사전 파싱 시 통일) 🧩목업 |

---

## 7. 외부 API 검증 결과와 사용 계약

### 7.1 판정 요약 (2026-07-02 실호출 — 상세 로그 부록 A)

| API | 용도 | 판정 | 비고 |
|---|---|---|---|
| KTO **KorService2** `locationBasedList2` | 대회장 반경 POI (관광지12·음식점39·숙박32·축제15) | **✅실측 사용 가능** | `dist` 거리순(`arrange=E`) 확인 |
| KTO `searchFestival2` | 날짜 기반 축제 (M3·S1 홈) | **✅실측 사용 가능 (함정)** | `eventStartDate` 필수. **구 `areaCode`는 에러 없이 0건** — 지역필터는 `lDongRegnCd`만 |
| KTO `detailCommon2` | POI 상세 enrich | ✅실측 | homepage 등 |
| KTO ~~KorService1~~ | — | **❌ 폐기** | HTTP 500 — 구버전 예제 금지 |
| KTO **Durunubi** `courseList` | 걷기 코스 (M4) | **✅실측 (제약)** | **좌표·반경 파라미터 없음** → GPX 사전 파싱으로 해결 완료 🧩목업 |
| KTO **WellnessTursmService** | 회복형 POI (M1 noHard) | **✅실측** | 반경+테마 동시 필터 확인. **data.go.kr 페어 키로만 동작** (구 hex 키 403) |
| 카카오 **로컬** keyword/category/address | 지오코딩·상권 POI·숙소·걷기 스팟 | **✅실측** | CSV 좌표와 정합 확인 |
| 카카오 **모빌리티** `/v1/directions` | 이동시간 (A5) | **✅실측** | kakao.com REST 키로 동작 (문서와 달리 별도 키 불필요 — 실측 우선). **자동차 전용, 도보 없음** |
| 카카오 **지도 JS SDK** | 프론트 지도 | **✅키 유효** | **도메인 등록 필수** (localhost:5173 + 배포 도메인) |
| 카카오 **로그인** | U1·U2 🆕회의 **P0 승격** | 📄문서 | JS 인가(`kauth.kakao.com/oauth/authorize`) → **토큰 교환은 서버**(REST 키+client_secret). 심사 불필요 |
| 카카오 공유·내비 | A4·A6 | 📄문서 | JS 키·도메인 등록만으로 사용 가능 |
| **이메일 발송 (SMTP/발송 서비스)** | U2·U3 🆕회의 | 신규 요건 | 인증 코드·임시 비밀번호 발송. 공급자 선정 🔧정책(§9.2) |

### 7.2 KTO TourAPI 요점

- 공통: `serviceKey` 쿼리 인증 · `MobileOS=ETC`·`MobileApp=runtrip` 필수 · `_type=json` · 쿼터 **개발계정 오퍼레이션당 일 1,000건** 📄문서 · **radius ≤ 20,000m** · 데이터 갱신 일 1회.
- **키는 data.go.kr 인코딩/디코딩 페어 키로 단일화** ✅실측 (KorService2·두루누비·웰니스 3종 모두 커버. 구 hex 키는 웰니스 403).
- 에러 형태: 401 키 오류 / 403 해당 API 활용신청 안 됨 / 500 없는 경로·구버전 / 쿼터 초과 등 포털 오류는 **`_type=json`이어도 XML**(`<OpenAPI_ServiceResponse>`) — JSON 파서와 별도 처리 필수 / 정상 `resultCode=="0000"`.
- KorService2: 신 분류체계만 신뢰 — 지역 `lDongRegnCd/lDongSignguCd`(법정동), 분류 `lclsSystm1~3`. 코드 사전은 `ldongCode2`·`lclsSystmCode2`로 조회(특별자치도 전환 이력 — 하드코딩 금지).
- `locationBasedList2` contentTypeId: 12 관광지 · 14 문화시설 · 15 행사/축제 · 25 여행코스 · 28 레포츠 · 32 숙박 · 38 쇼핑 · 39 음식점. ✅실측: contentTypeId=15는 반경 검색 되지만 **응답에 날짜 필드 없음** → 축제는 `searchFestival2`(날짜 있음, 반경 없음)와 조합(§8.3).
- 웰니스: `langDivCd=KOR` 필수. `wellnessThemaCd`: EX050100 온천/사우나/스파 · EX050200 찜질방 · EX050300 한방 · EX050400 힐링명상 · EX050500 뷰티스파 · EX050600 기타 · EX050700 자연치유. 응답 좌표는 대문자 `mapX/mapY`, 이미지는 매뉴얼 표기와 달리 `firstimage/firstimage2` ✅실측(실측 우선). 희소 카테고리 — 기본 반경 20km 🔧정책.
- 두루누비: `courseList`(brdDiv=DNWW 걷기길) — 좌표는 `gpxpath` GPX 파싱으로만 확보 → **사전 파싱본 261코스로 해결 완료** 🧩목업.

### 7.3 카카오 API 요점

| 표면 | 키 | 사용처 |
|---|---|---|
| 클라이언트 | **JavaScript 키** (`design/.env` `VITE_KAKAO_MAP_KEY`) | 지도 SDK(+services 검색), 공유, 내비, **로그인 인가** |
| 서버·프록시 | **REST 키** (루트 `.env` `KAKAO_REST_KEY`) | 로컬 검색·지오코딩·모빌리티·**로그인 토큰 교환** 🆕 |

- 로컬 API: `keyword.json`(query, x/y/radius≤20000, sort=distance, size≤15, page≤45) · `category.json`(category_group_code 1개 필수) · `address.json`. 사용 코드: **AT4 관광명소 · AD5 숙박 · FD6 음식점 · CE7 카페**.
- JS SDK는 콘솔 Web 플랫폼 **도메인 등록 필수** — `http://localhost:5173`(design, strictPort 고정) + 배포 도메인. 쿼터 초과 시 429(1회 재시도 후 폴백, NFR-5).
- 모빌리티: 자동차 전용(도보 없음) — 도보는 직선거리×4km/h 추정 라벨 또는 생략 🔧정책. waypoints ≤5.
- 로그인: 인가는 JS, **토큰 교환·회원 매핑은 백엔드**(§9.3). "친구에게 메시지"만 별도 검수 — 사용 안 함.

### 7.4 함정·제약 종합 (구현 체크리스트)

1. **KorService1 금지** — HTTP 500 ✅실측.
2. **areaCode 함정** — `searchFestival2`에 구 `areaCode`는 **에러 없이 0건** ✅실측. `lDongRegnCd`만.
3. KTO 포털 오류는 `_type=json`이어도 **XML** — 파싱 전 Content 검사.
4. 쿼터 오퍼레이션당 일 1,000건(개발) — §9.5 예산 준수.
5. radius ≤ 20km — 초과는 다중 호출.
6. `searchFestival2` 반경 불가 + `locationBased(15)` 날짜 없음 — M3는 조합 설계(§8.3).
7. 두루누비 좌표 없음 — GPX 사전 파싱(완료).
8. 웰니스는 data.go.kr 페어 키 전용 (구 hex 403). 필드 대소문자·이미지 필드 상이.
9. 좌표 순서 — REST `x=lng,y=lat` vs SDK `LatLng(lat,lng)` (§6.6). 경계 변환 단일화.
10. 카카오 JS SDK 도메인 미등록 시 무동작 — 배포 도메인 추가 잊지 말 것.
11. 모빌리티 도보 없음.
12. 카카오 로컬 size≤15·page≤45 = 최대 675건/쿼리.
13. CSV 멀티라인 파서 필수 (행수≠대회수) ✅실측.
14. **serviceKey 인코딩 함정** — URL 직결 시 인코딩 키(`KTO_SERVICE_KEY_ENC`), 라이브러리 인코딩 시 디코딩 키(`KTO_SERVICE_KEY`). 혼용 시 인증 실패.
15. 🆕 브라우저에서 KTO·카카오 REST 직호출 금지(CORS + 키 노출) — 반드시 프록시 경유(`/api/kto`·`/api/kakao`, §9.1~9.2). 클라이언트 코드는 프록시 경로만 안다.

---

## 8. 기능별 데이터 조달 설계

### 8.1 POI 하이브리드 전략 (M1)

실측 근거: 세종 기준 KTO 숙박 반경 10km 4곳 vs 카카오 음식점 반경 2km 856곳. KTO는 관광 품질 데이터(공식 이미지·overview·분류)가 강함.

| catKey | 1순위 | 2순위(보강/폴백) | 비고 |
|---|---|---|---|
| tour | **KTO** locationBasedList2 (12) | 카카오 AT4 | 공모전 필수요건 축 |
| history | **KTO** 12 + lclsSystm 역사 분류 | 카카오 kw | |
| food | **카카오** FD6 | KTO 39 | 커버리지 우선 |
| cafe | **카카오** CE7 | — | KTO 대응 타입 없음 |
| lodging | **카카오** AD5 🆕회의 확정 | KTO 32·searchStay2 | KTO 숙박 희소 ✅실측 |
| wellness | **KTO 웰니스** + `wellnessThemaCd=EX050100·0200·0500·0700` | 카카오 kw (희소 지역 보강) | noHard 동선 핵심, 기본 반경 20km |
| nature | 카카오 kw + **두루누비**(§8.4) | KTO 12 자연 분류 | S8 러닝코스와 공유 |

- 클라이언트 런타임 폴백 체인 🧩목업: **① live(카카오 실시간, 반경 8km 거리순) → ② sample(사전수집 `PRESAMPLED[raceId][catKey]`) → ③ synth(합성)** — 소스 배지 노출.
- 반경 🔧정책: 기본 8km → wellness·nature 등 희소 카테고리는 결과 <3건이면 20km 확장 재검색.
- 사전수집: 대회당 카테고리별 20건 저장, 프론트 8건 노출 🔧정책. 산출물 `design/public/data/pois/{raceId}.json`. KTO POI는 `contentid` 보존 → `detailCommon2` 지연 enrich.

### 8.2 M2 대회 데이터 파이프라인 (구현 완료 ✅)

```
크롤 CSV (마라톤온라인+마라톤GO, 271행)
  → backend/build_races_json.py
     · RFC4180 파서 · 종목 표준화(has_* 우선) · region 17종 보정('충청'→보정)
     · 좌표 CSV값(100%), 누락 시 geocode.py
     · 중복 병합(정규화명+date, 117쌍): 최근 확인일 우선 + 빈 필드 상호 보충 + 출처 병기
  → design/public/data/races.json (153건, ~100KB)
  → 프론트: fetch → normalizeRaces() (실패 시 내장 샘플 폴백 = NFR-1)
```

- 종목 플래그 all-False 45건 🔧정책: 트레일·걷기는 캘린더 노출하되 위저드 종목은 5K 폴백 + "종목 정보 없음" 표시.
- 접수상태는 표시 시점에 `regStatusOf`로 재계산(§5.5) — 재크롤 주기 🔧정책 주 1회(확인일 노출로 신뢰성 보완).

### 8.3 M3 축제 조달

- **현행(데모) 🧩목업**: 클라이언트가 프록시 경유 `searchFestival2`를 **대회일 ±14일** 윈도우로 라이브 조회 → Haversine 반경 40km 필터 → 거리순 6건 (S3 상세).
- **배포/확장 대안** (쿼터 절약·안정성): `searchFestival2` 전량 1회 수집(과거 60일 기준) → 대회별 로컬 매칭(기간 겹침 ∧ ≤20km) → `races.json`에 `festivals[]` 주입. 홈(S1) 축제 섹션도 동일 수집분 재사용 🔧정책.

### 8.4 M4 두루누비 코스 조달 (구현 완료 ✅ 🧩목업)

1. `courseList?brdDiv=DNWW` 전량 수집 → 각 코스 `gpxpath` GPX 다운로드 → 트랙포인트 추출.
2. 산출물 `design/src/data/durunubi_courses.json` — **261코스** `{id, name, distKm, level, minutes, cycle, sigun, sido, summary, points}`.
3. 런타임 매칭은 §5.8(`buildRouteNear` 왕복 빌더 + `browseCourses`). 도시 공백은 §5.9 걷기 스팟 보강.
4. 동선(M1)과의 연계는 S7 연계 카드(§4.10) — walk 블록 제거 이후의 접점 🆕회의.

### 8.5 이동시간(A5)·지오코딩

- 이동시간: 일자 blocks 좌표 순서로 모빌리티 directions(waypoints ≤5) 1콜 → 블록 사이 duration 라벨. 실패 시 표시 생략(비차단) 🔧정책. (G-10)
- 지오코딩: CSV 좌표 100% — 신규 수집분만 `backend/geocode.py`(키워드→주소 폴백, 캐시) 재사용.

---

## 9. 시스템 아키텍처

### 9.1 현행 데모 아키텍처 🧩목업

**정적 데이터 + dev 프록시 + 클라이언트 라이브 폴백.**

```
[배치 (로컬 실행)]  crawl CSV → build_races_json.py → races.json (153)
                    두루누비 GPX 사전 파싱 → durunubi_courses.json (261)
[design/ 프론트 (Vite, port 5173 고정)]
   ├ races.json·durunubi_courses.json 로드 (실패 시 내장 샘플 = NFR-1)
   ├ 카카오맵 JS SDK (키 없으면 SVG 폴백)
   └ vite dev proxy — 키를 서버측에서 주입, 브라우저 무노출:
       /api/kto/*   → apis.data.go.kr/B551011/* (+serviceKey = TOUR_API_KEY)
       /api/kakao/* → dapi.kakao.com/*        (+Authorization: KakaoAK KAKAO_REST_KEY)
```

- 포트는 **5173 strictPort** (카카오 콘솔 등록 도메인과 일치 — 포트 밀림 방지). REQUIREMENTS·design/README의 "5180" 표기는 구식(N-11).

### 9.2 최종 목표 아키텍처 🆕회의 — **백엔드 필수**

회의로 확정된 회원(U1~U3)·커뮤니티(C1~C2)·보관함 영속화(B1)·조회수 집계(H1)는 서버 없이는 불가능하다. SPEC v1의 "서버 없는 정적 파이프라인"은 **데모 폴백 경로로 강등**하고, 다음을 목표 구성으로 한다.

```
[클라이언트 SPA]  design/ 목업 구조 승계 (탭 5개 + 위저드)
      │  /api/*  (동일 경로 계약 — vite proxy를 백엔드가 대체하므로 클라 코드 무변경)
[백엔드 서버]  (예: Spring Boot 🔧정책 — 팀 스택 기준, 확정은 구현 착수 시)
      ├ Auth: 이메일 가입·로그인(JWT/세션 🔧정책), 이메일 인증, 임시 비밀번호, 카카오 OAuth 토큰 교환
      ├ 메일 발송: SMTP/발송 서비스 (인증 코드·임시 비밀번호) 🔧정책
      ├ 보관함: trips / savedCourses CRUD (userId 귀속)
      ├ 커뮤니티: posts CRUD + 지역·타입 필터 (+신고 최소 기능)
      ├ 대회: races 제공 + 조회수 집계(raceView)
      └ 프록시: /api/kto, /api/kakao — 키 은닉 (기존 경로 계약 유지)
[DB]  users · emailAuth · trips · savedCourses · communityPosts · raceViews (+ races 캐시)
```

### 9.3 백엔드 API 초안 🆕회의 🔧정책

| 영역 | 엔드포인트 (초안) |
|---|---|
| 인증 | `POST /auth/signup` · `POST /auth/email/send-code` · `POST /auth/email/verify` · `POST /auth/login` · `POST /auth/password/reset-request` · `POST /auth/password/reset` · `POST /auth/kakao` |
| 회원 | `GET /me` · `PATCH /me` (닉네임 등) |
| 보관함 | `GET/POST/DELETE /me/trips` · `GET/POST/PATCH/DELETE /me/courses` (kind=saved\|ran) |
| 커뮤니티 | `GET /posts?type=&region=` · `POST /posts` · `GET /posts/{id}` · `POST /posts/{id}/save` · `POST /posts/{id}/report` |
| 대회 | `GET /races` · `GET /races/{id}` · `POST /races/{id}/view` (조회수) · `GET /races/popular` · `GET /races/closing-soon` |
| 프록시 | `ALL /api/kto/*` · `ALL /api/kakao/*` (기존 계약 그대로) |

### 9.4 키·시크릿 운영

- 루트 `.env`(gitignore ✅): `KTO_SERVICE_KEY`(디코딩)·`KTO_SERVICE_KEY_ENC`(인코딩)·`KAKAO_REST_KEY`·`KAKAO_JS_KEY`. `design/.env`: `VITE_KAKAO_MAP_KEY`(JS 키)·`TOUR_API_KEY`·`KAKAO_REST_KEY`(dev 프록시 주입용).
- **KTO 키는 data.go.kr 페어 키로 단일화** ✅실측. REST 키·KTO 키는 클라이언트 번들 포함 금지 — JS 키만 클라 노출(도메인 제한 보호).
- 🆕 추가 시크릿: 메일 발송 계정/키, JWT 서명 키, 카카오 client_secret — 전부 서버 전용.

### 9.5 KTO 쿼터 예산 (개발계정 1,000건/일/오퍼레이션)

| 작업 | 오퍼레이션 | 호출량 | 판정 |
|---|---|---|---|
| 축제 전량 수집 | searchFestival2 | ~10콜 | 여유 |
| 두루누비 전량 수집 | courseList | ~20콜 + GPX(쿼터 외) | 여유 (완료) |
| 대회별 POI (tour·history) | locationBasedList2 | 271×2 = 542콜 | 1일 내 가능 |
| 대회별 POI (food·lodging 보강) | locationBasedList2 | +542 → 합 1,084 | 초과 → 이틀 분할 또는 접수중 우선 🔧정책 |
| 웰니스 대회별 | Wellness locationBasedList | 271콜 | 여유 |
| 상세 enrich | detailCommon2 | 노출분만 지연 호출 | 여유 |
| 🆕 S3 라이브 축제 조회 | searchFestival2 | 상세 진입당 1콜 | 데모 여유 — 트래픽 증가 시 §8.3 사전수집 전환 |

재수집은 `modifiedtime` 증분. 운영계정 전환(승인 1~3일)은 제출 직전 신청.

---

## 10. 비기능 요구사항

| ID | 요구사항 |
|---|---|
| NFR-1 | **키 없는 데모 동작**: 카카오 키·네트워크 부재 시에도 SVG 지도 + sample/synth 폴백으로 **탐색·동선 플로우** 전체 시연 가능 (현행 유지). 회원·커뮤니티·보관함 동기화는 서버 필요 — 게스트 범위는 [미결-6] |
| NFR-2 | **폴백 투명성**: POI 소스 배지(live/sample/synth) 노출 |
| NFR-3 | **에러 격리**: 외부 API 실패는 해당 블록 place=null 강등 — 동선 생성 자체는 실패하지 않음 |
| NFR-4 | **KTO 응답 방어**: resultCode≠0000·XML 오류·401/403/500 구분 로깅 |
| NFR-5 | **429 대응**: 카카오 실패 시 1회 재시도 후 폴백. 파이프라인은 호출 간 지연 + 캐시 |
| NFR-6 | **신뢰성 표기**: 대회 카드·상세에 출처·최근확인일 상시 노출 |
| NFR-7 | **저작권**: KTO 이미지 `cpyrhtDivCd` 표기 준수, "한국관광공사" 크레딧 (축제 섹션 적용 완료 🧩목업) |
| NFR-8 | **좌표 규율**: §6.6 위반 금지 — 경계 변환 함수 단일화 |
| NFR-9 🆕 | **비밀번호 보안**: 단방향 해시(bcrypt/argon2) 저장. 평문·가역 암호화 금지. 임시 비밀번호도 발급 즉시 해시만 보관, 원문은 메일로만 전달 |
| NFR-10 🆕 | **인증 코드 보호**: 6자리 코드 10분 만료 · 검증 5회 제한 · 재발송 쿨다운 60초 · 코드 무차별 대입 방지 |
| NFR-11 🆕 | **세션/토큰**: JWT(액세스+리프레시) 또는 서버 세션 🔧정책. 로그아웃·만료 처리 명세 필수 |
| NFR-12 🆕 | **개인정보**: 최소 수집(이메일·닉네임·비밀번호 해시), 필수/선택 동의 분리 + 동의 이력 저장, 탈퇴 시 개인정보 삭제. 카카오 프로필 수집 항목 최소화 |
| NFR-13 🆕 | **UGC 안전장치**: 커뮤니티 게시물 신고·관리자 삭제 경로 확보. 업로드 경로 좌표는 한국 영역 범위 검증 🔧정책 |

---

## 11. 구현 백로그 (우선순위)

**완료 (v1 백로그 정리)**

| ID | 항목 | 비고 |
|---|---|---|
| G-00 | 웰니스 API 활용신청 + 키 단일화 | ✅ 07-02 |
| G-01 | 실데이터 races.json 153건 연결 | ✅ 07-02 |
| G-02 | TODAY 하드코딩 제거 (D-day·접수상태 오늘 기준) | ✅ 07-02 |
| G-03 | 인근 축제 실연동 | ✅ 07-02 클라 라이브(프록시) — 배포 시 §8.3 전환 검토 |
| G-05 | 두루누비 실데이터 + 러닝코스 빌더 | ✅ 07-02 (261코스 사전 파싱) |
| G-08 | 홈(현 캘린더) 캘린더 뷰 실구현 | ✅ 07-02 |
| G-13 | walkDesc 무효 코드 | N-06(산책 블록 제거)으로 소멸 |

**P0 — 회의 반영 + 공모전 MVP 필수** 🆕회의

| ID | 항목 | 근거 |
|---|---|---|
| N-01 | 백엔드 기반 구축 (인증·DB·메일 발송·API 프록시 이관) | §9.2 |
| N-02 | A1~A3 인증 화면 3종 (이메일 가입+인증, 로그인, 임시 비밀번호) | §4.1~4.3 |
| N-03 | 카카오 로그인/가입 연동 | §4.1·§7.3 |
| N-04 | S2 필터 모달 개편 (키로수·모집마감·지역 + 적용 칩 + 취소/완료) | §4.5 |
| N-05 | 탭바 5탭 개편 (홈·캘린더·러닝코스·커뮤니티·보관함, 동선 탭·route 화면 삭제) | §2.1 |
| N-06 | 엔진 walk 블록 3종 + S7 산책 섹션 제거, S7→S8 연계 카드 추가 | §4.10·§5.6 |
| N-07 | S10 보관함 (동선/러닝코스 세그먼트 + 서버 영속화 — 구 G-14 대체) | §4.13 |
| N-08 | S1 홈 신설 (검색·아이콘·마감임박·인기·축제) | §4.4 |
| G-04 | 전 대회 POI 사전수집 자동화 (KTO+카카오, PRESAMPLED 대체) | §8.1 |
| G-06 | 카카오 콘솔 도메인 등록 (localhost:5173 + 배포 도메인) | §7.3 (사용자 작업, 1분) |

**P1**

| ID | 항목 | 근거 |
|---|---|---|
| N-09 | S9 커뮤니티 MVP (동선 공유 피드 + 코스 공유 — 범위 [미결-4] 확정 후) | §4.12 |
| N-10 | 인기 대회 조회수 집계 (`/races/{id}/view` + popular) | §4.4 [미결-2] |
| G-07 | 소스 배지 S7 결과 화면 렌더 정리 | NFR-2 |
| G-09 | 카톡 공유 구현 (대회 feed·동선 list 템플릿) | A4 |
| G-10 | 모빌리티 이동시간 라벨 | A5 §8.5 |
| G-11 | `imageUrl` 활용 (S3 히어로·카드 썸네일 — 133건 보유) | §6.1 |
| G-12 | KTO POI 상세 enrich (detailCommon2) | §8.1 |
| N-11 | 문서·설정 정리: design/README 포트 표기(5180→5173), REQUIREMENTS.md 아카이브 | §9.1 |

**P2**

| ID | 항목 |
|---|---|
| N-12 | 구글·네이버 로그인 🆕회의 "시간 남으면" |
| N-13 | 커뮤니티 코스 → 러닝코스 '내 주변' 병합 노출, GPS 뛴 기록([미결-5]) |
| G-15 | 카카오내비 연발·반려동물 필터(A7)·다국어(A8) |

---

## 12. 미결 사항 (다음 회의 안건)

| # | 안건 | 배경 / 제안 🔧정책 |
|---|---|---|
| 1 | **캘린더 탭 최종 라벨** | 회의에서 "캘린더 이름 변경(네비게이션바)"만 확정되고 목표 라벨 미기록. 후보: "캘린더" 유지 / "대회" / "대회 일정". 홈 신설로 역할이 '대회 캘린더'로 좁혀진 점 반영해 결정 |
| 2 | **인기 대회 산정 기준** | 조회수 서버 집계(N-10) 도입 전 MVP 대안: 접수중 가중 + 찜 수(도입 시) + 마감 임박 제외 등 |
| 3 | **필터 '키로수' UI** | 제안: 종목 4버킷(5K/10K/하프/풀) 복수 선택. 대안: 자유 거리 슬라이더(러닝코스와 통일감) |
| 4 | **커뮤니티 MVP 범위** | 코스 등록 방식(두루누비 슬라이스 공유/지도 그리기/GPX 업로드), 좋아요·댓글 포함 여부, 신고 처리 절차 |
| 5 | **'내가 뛴 코스' 판정** | 제안: 수동 완료 체크(MVP). 대안: GPS 러닝 기록(대형 기능 — 별도 로드맵) |
| 6 | **게스트(비로그인) 범위** | 제안: 탐색(S1·S2·S3·S8) 허용, 저장·커뮤니티·보관함은 로그인 유도 |
| 7 | **임시 비밀번호 방식 확정** | 회의 결정(임시 비밀번호 발송) 유지 시: 첫 로그인 비밀번호 변경 강제 여부. 대안: 재설정 링크 방식(보안상 표준) |
| 8 | **홈 '관광' 아이콘 목적지** | 제안: 홈 축제 섹션 스크롤(MVP) → 전용 관광 탐색 화면(확장) |
| 9 | **대회 찜(하트) 기능** | 현재 `[표시 전용]`. 보관함에 '찜한 대회' 추가 여부 및 인기 대회 산정(#2)과의 연계 |

---

## 13. 로드맵 (제안서 4단계 + 회의 반영)

| 단계 | 기간 | 내용 | KPI |
|---|---|---|---|
| ① MVP (공모전 제출) | ~4M | **P0 전부** = 회원(이메일+카카오)·홈·5탭 개편·필터 모달·보관함 + 기존 대회/동선/러닝코스. 수도권·광역시 100+ 대회 | 베타 MAU 3,000 · 일정 저장률 20% · 동선 채택률 15% |
| ② 전국 확장·검증 | 4~12M | 300+ 대회, **커뮤니티 정착(C1·C2)**, 편집 이력 수집→룰 개선 | 재방문 25% · 편집 이력 1,000건 · 코스 공유 게시물 500건 🆕 |
| ③ 지자체·상권 연계 | 12~24M | 관광 전환 리포트, 숙박·웰니스 제휴, 축제 결합 코스 | 제휴 지역 5 · 업체 100 · 전환율 10% |
| ④ 인바운드·확장 | 24M~ | 다국어(웰니스 langDivCd), 외국인 러너, 트레일러닝 | 외국인 10% · 글로벌 MAU 30,000 |

---

## 부록 A. 실호출 검증 로그 (2026-07-02)

| # | 호출 | 결과 |
|---|---|---|
| 1 | KorService2 `locationBasedList2` mapX=127.2714 mapY=36.4912 radius=5000 contentTypeId=12 arrange=E | 0000 OK — 세종호수공원 dist 872m, firstimage 포함 |
| 2 | 〃 contentTypeId=39 radius=3000 | 0000 OK — 음식점 dist 정렬 |
| 3 | 〃 contentTypeId=15 radius=20000 | 0000 OK totalCount=12 — **날짜 필드 없음** |
| 4 | 〃 contentTypeId=32 radius=10000 | 0000 OK totalCount=**4** (숙박 희소) |
| 5 | `searchFestival2` eventStartDate=20260701 | 0000 OK — eventstartdate/enddate 포함 |
| 6 | 〃 + `areaCode=1` (구 파라미터) | 0000 **totalCount=0 (조용한 실패)** |
| 7 | 〃 + `lDongRegnCd=11` | 0000 OK — 서울 축제 정상 필터 |
| 8 | `detailCommon2` contentId=1946955 | 0000 OK — homepage 등 |
| 9 | KorService1 locationBasedList1 | **HTTP 500** (폐기) |
| 10 | Durunubi `courseList` brdDiv=DNWW | 0000 OK — 전 필드 확보 (gpxpath 포함) |
| 11 | WellnessTursmService locationBasedList/areaBasedList (구 hex 키) | **HTTP 403 Forbidden** |
| 12 | (대조) 가짜 서비스명 / 가짜 키 | 500 / **401** → 403=미신청 확정 |
| 13 | 카카오 keyword "세종중앙공원" | 200 — CSV 좌표와 일치 |
| 14 | 카카오 category FD6 radius=2000 | 200 — total_count=856 |
| 15 | 카카오 address "세종특별자치시 연기면" | 200 |
| 16 | 모빌리티 directions 세종 구간 | 200 result_code=0 — 5,598m/700초/택시 8,300원 |
| 17 | 지도 SDK sdk.js?appkey=JS키 | 200 — 정상 JS 서빙 |
| 18 | 웰니스 locationBasedList × **data.go.kr 페어 키** (활용신청 후) | 0000 OK — 세종 20km totalCount=4, `firstimage` 확인 |
| 19 | 〃 `wellnessThemaCd=EX050100` 서울 20km | 0000 OK — 3건 전부 EX050100 (무필터 대조 9건) → 테마필터 동작 |
| 20 | 웰니스 × 구 hex 키 (재확인) / KorService2·Durunubi × 페어 키 | 403 유지 / 둘 다 0000 OK → **페어 키 단일화 결정** |

## 부록 B. 코드표 요약

- **KTO contentTypeId**: 12 관광지 · 14 문화시설 · 15 행사/축제 · 25 여행코스 · 28 레포츠 · 32 숙박 · 38 쇼핑 · 39 음식점
- **wellnessThemaCd**: EX050100 온천/사우나/스파 · EX050200 찜질방 · EX050300 한방 · EX050400 힐링명상 · EX050500 뷰티스파 · EX050600 기타 · EX050700 자연치유
- **카카오 category_group_code**(사용분): AT4 관광명소 · AD5 숙박 · FD6 음식점 · CE7 카페
- **arrange**(KTO): A 제목순 · C 수정일순 · D 생성일순 · **E 거리순**(위치기반)
- **두루누비**: brdDiv DNWW 걷기길/DNBW 자전거길 · crsLevel 1하(쉬움)/2중(보통)/3상(어려움)
- **lDongRegnCd**: 법정동 시도 2자리 (서울=11 ✅실측). 전체 목록은 `ldongCode2`로 조회 — 하드코딩 금지
- **걷기 스팟 필터** 🧩목업: 포함 `공원|관광|명소|산책|둘레|하천|유원지|수목원|숲|생태|휴양|호수|해수욕|해변|등산로|트레킹|자연` / 제외 `화장실|주차장|주차|테니스|풋살|축구장|야구장|농구장|체육관|관리사무소|매점|안내소|정류장`

## 부록 C. 2026-07-16 회의 결정 ↔ 명세 반영 매핑

| # | 회의 결정 (원문 요지) | 반영 위치 |
|---|---|---|
| 1 | 로그인 화면 (회원가입/비밀번호찾기 진입) | §4.1 A1 · U1 |
| 2 | 회원가입 — 카카오 + 이메일 가입(이메일·비번·닉네임 → 이메일 인증 → 가입), 정보 동의 필수, 구글·네이버는 추후 | §4.2 A2 · U2 · N-12(P2) |
| 3 | 비밀번호 찾기 — 이메일 인증으로 새 비밀번호 생성·안내 | §4.3 A3 · U3 · [미결-7] |
| 4 | 홈 화면 — 최상단 검색 + 기능 아이콘(달력·지도·코스·관광) + 마감임박 + 조회 많은 대회 + 축제(지역 관광) 추천. 검색 먼저 → 메인 기능으로 | §4.4 S1 · H1 · [미결-2·8] |
| 5 | 캘린더 이름 변경(네비게이션바) | §2.1 · [미결-1] (목표 라벨 미기록) |
| 6 | 캘린더 필터 — 버튼 1개 → 모달(키로수·모집마감여부·지역) → 완료 시 선택 칩 나열, 취소 버튼 | §4.5 · F1 · N-04 · [미결-3] |
| 7 | 숙소는 카카오 API로 | §4.9 · §8.1 lodging (현행 구현과 일치 — 확정) |
| 8 | 동선 만들기에서 숙소 주변 산책코스 제거 (러닝코스 기능이 담당) | §4.10 · §5.6 엔진 v2 · N-06 |
| 9 | 네비게이션바에서 동선 삭제 — 캘린더에서 날짜 골라 진입 | §2.1 · §4.5 캘린더 뷰 · N-05 |
| 10 | 커뮤니티 — 저장 동선 공유 + 두루누비 외 코스 추천 + 지역 설정 기반 유저 코스 공유 | §4.12 S9 · C1·C2 · N-09 · [미결-4] |
| 11 | '내 여행' → '보관함' — 대회 동선 + 내가 뛰거나 저장한 러닝코스 | §4.13 S10 · B1 · N-07 · [미결-5] |
