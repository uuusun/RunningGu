# 런닝구 — 공통 작업 지침

**Codex · Claude Code 공용.** 규칙을 바꾸려면 이 파일을 고친다.
(`CLAUDE.md` 는 이 파일을 불러오는 한 줄짜리다. 거기에 내용을 복사하지 말 것.)

한국어로 답한다. 커밋 메시지 · PR · 주석도 한국어다.

> 배경 설명 · OS 별 설정 · 저장소 구조 상세는 [`docs/agent-reference.md`](docs/agent-reference.md).
> 이 파일에는 **매 작업마다 필요한 것만** 둔다. 길어지면 뒤가 읽히지 않는다.

---

## 0. 30초 요약

| | |
|---|---|
| 기준 브랜치 | `develop` (`main` 은 릴리스 전용) |
| 최종 기준 문서 | `SPEC.md` |
| 빌드 | `cd android && ./gradlew :app:testDebugUnitTest` (JDK 21) |
| 막히면 | 연결부 · 스키마 · 범위 관련이면 **묻는다.** 그 외는 가정을 밝히고 진행 |
| 끝낼 때 | 구현 + 테스트 + **문서 반영**까지가 완료 |

**만드는 것** — 런닝구(區). 안드로이드 네이티브 앱(Kotlin · Compose · MVVM) + 백엔드(Spring Boot · PostgreSQL).
마라톤 일정 통합 + 대회 전후 여행 동선 추천 + 위치 기반 러닝코스. 2026 관광데이터 공모전 출품작.

**커뮤니티 · 인기 대회 집계 · 산책 블록은 범위에서 뺐다.** 되살리자는 제안을 하지 않는다.
(전체 목록은 참고 문서 1장)

---

## 1. 문서 우선순위와 판단 기준

충돌하면 위가 이긴다.

| 문서 | 지위 |
|---|---|
| `SPEC.md` | **최종 기준.** 제품 범위·정책의 SSOT. §5 규칙 값은 "포팅 시 변경 금지" |
| `docs/files/런닝구_API_명세서.md` | **HTTP API 계약.** 백엔드 시드 계약 — 앱은 이걸 보고 만든다 |
| `docs/screen-api-matrix.md` | **화면 ↔ API 매핑표.** 화면마다 언제 무엇을 호출하고 어떤 필드를 쓰는지 + **미정 계약 목록**(§10) |
| `docs/mockup-design/런닝구-목업-v2.html` | 화면·상호작용 참고 |
| springdoc / Swagger | 백엔드 구현 후 **최종 실행 계약**이 된다 (아직 없음) |
| `CONVENTION.md` | Git · PR 규칙 |
| `docs/domain-logic-audit.md` | 원본 코드가 SPEC 보다 낡은 지점 대조표 |
| `docs/android-porting-plan.md` | 파일 단위 포팅 순서 |
| `reference-web/` | 참조 구현(JS). 문서와 다르면 **문서가 맞다** |

**연결부 작업 전에 `docs/screen-api-matrix.md` §10 을 먼저 본다.** 미정 25건이 표로 정리돼 있어서,
내가 정하려는 것이 이미 "결정 필요" 로 올라와 있는지 알 수 있다. 있으면 임의로 정하지 말고 묻는다.

**코드와 문서가 다르면 문서가 이긴다.** 코드가 옳다고 판단되면 문서를 먼저 고친다 — 코드만 고치고 넘어가지 않는다(5장).

### 요구사항이 불명확할 때

임의로 정하지 않는다. 다만 전부 물어보면 아무도 못 움직이므로 기준을 둔다.

| 상황 | 어떻게 |
|---|---|
| 연결부(API · `UiState` · DTO · enum) · DB 스키마 · 외부 계약 | **멈추고 묻는다** |
| 범위가 늘어나는 것 · 뺀 기능을 되살리는 것 | **멈추고 묻는다** |
| 새 라이브러리 추가 | **멈추고 묻는다** |
| 남의 담당 영역을 고쳐야 할 때 | **멈추고 묻는다** |
| 그 외 (되돌리기 쉬운 내부 구현) | **가정을 밝히고 진행.** 무엇을 가정했는지 보고에 남긴다 |

---

## 2. 아키텍처 불변 규칙

```
android/app/src/main/java/com/runninggu/app/
├── ui/        Compose 화면 + 화면별 ViewModel(StateFlow<UiState>)
├── domain/    §5 전체. 순수 Kotlin — Android import 금지
├── data/      remote(Retrofit·매퍼) · local(assets·Room·DataStore) · model(§6 계약)
└── util/
```

1. **`domain` 은 Android 를 모른다.** `android.*` · Compose · Context 를 import 하지 않는다.
2. **데이터 기준 저장소(SSOT)는 서버다.** Room 은 **읽기 캐시**일 뿐 — 양방향 동기화하지 않는다.
3. **외부 API 는 앱이 직접 부르지 않는다.** KTO · 카카오 REST 는 전부 우리 서버를 거친다.
   앱에 있는 키는 카카오 **네이티브** 키 하나뿐이다. `data/remote` 에 자체 백엔드 외의 호스트가
   등장하면 잘못된 것이다.
4. **좌표 변환과 날짜 해석은 `data/remote` 매퍼와 `domain/KstDates` 에서만.**
   비즈니스 날짜는 **KST**, timestamp 는 UTC `Z`.
5. **비동기 화면은 로딩 / 내용 / 빈 / 오류 네 상태를 모두 만든다**(§3-5).
   영역 단위 부분 실패가 가능하다 — 홈의 마감임박과 축제는 따로 실패할 수 있다.
6. **기존 패턴을 먼저 쓴다.** 네이밍은 `XxxScreen` · `XxxUiState` · `XxxViewModel` · `XxxDto` ·
   `toDomain()` / `toDto()`. 서버 오류는 RFC 9457 `problem+json` 의 `code` 로 분기한다.

---

## 3. 실행과 검증

```bash
export JAVA_HOME=$(/usr/libexec/java_home -v 21)   # Windows 는 참고 문서 3장
cd android
./gradlew :app:testDebugUnitTest     # 단위 테스트
./gradlew :app:assembleDebug         # 디버그 빌드
```

`android/local.properties` 에 `sdk.dir` 이 필요하다(gitignore 대상, 각자 생성).

### 작업 종류별 최소 검증

| 바꾼 것 | 최소한 이것은 돌린다 |
|---|---|
| `domain/` | `:app:testDebugUnitTest` — **테스트 없이 머지하지 않는다** |
| `data/` | `:app:testDebugUnitTest` + 매퍼 단위 테스트 |
| `ui/` | `:app:assembleDebug` + 화면 스크린샷 |
| `scripts/` | 해당 스크립트 2회 실행 후 결과가 같은지(결정성) |
| 문서만 | 없음 |

**린트 · 포맷터는 아직 없다.** "린트 통과" 를 완료 조건으로 삼지 않는다.

---

## 4. 파트 사이를 잇는 것은 계약을 먼저 쓴다

파트를 나눠 각자 만들면 합칠 때 시간이 다 날아간다. 서로의 코드를 읽어야 상대가 뭘 만들었는지
알 수 있기 때문이다. **연결부는 구현보다 계약을 먼저 확정한다.**

**연결부** = 한쪽이 만들고 다른 쪽이 쓰는 것 전부. HTTP API(경로 · 파라미터 · 응답 필드 ·
에러 `code` · 페이징) · `UiState` · DTO 매핑 · enum 값 · 데이터 파일 스키마.

1. **주인은 "먼저 만든 사람" 이 아니라 "주는 쪽"(producer) 이다.**
   HTTP API 는 서버가, `UiState` 는 데이터를 채우는 ViewModel 쪽이 주인이다.
   받는 쪽은 **리뷰어**다 — 따르기만 하는 게 아니라 이의를 제기할 수 있다.
   먼저 코딩했다는 이유로 기준이 되지 않는다. 그러면 설계가 아니라 경주가 된다.
2. **계약은 코드가 아니라 문서에 산다.**
   **남의 구현 코드를 읽어서 계약을 알아내는 상황이 되면 이미 실패한 것이다.**
3. **계약이 없는데 지금 필요하면, 막지 말고 순서만 지킨다.**
   초안이어도 좋으니 계약을 먼저 PR 로 올리고 곧바로 구현을 이어간다. 리뷰는 하루 안에.
4. **계약 변경은 반드시 PR 로.** 구현하다 슬쩍 바꾸지 않는다. 바꾸면 쓰는 사람에게 알린다.
5. **계약과 구현이 다르면 계약이 이긴다.** 구현이 옳으면 계약을 먼저 고친다.
6. **계약이 정해지면 기다리지 않고 병렬로 간다.** 받는 쪽은 가짜 구현(mock)으로 진행한다.

### 책임 경계

| 경계 | 주는 쪽 | 받는 쪽 |
|---|---|---|
| HTTP API | 백엔드(유선경) | 앱(이건모) |
| `UiState` | ViewModel(이건모) | Compose 화면(김민지) |
| 도메인 모델 ↔ DTO | `data/remote` 매퍼(이건모) | — |

---

## 5. 구현하다 명세와 달라졌으면 같은 PR 에서 고친다

가장 자주 새는 자리다. 코드만 고치고 문서를 두면 다른 파트가 낡은 명세를 보고 만든다.

1. **명세 수정은 같은 PR 에 넣는다.** "나중에 문서 정리 PR 올리겠다" 는 안 지켜진다.
2. **대화 중에 결정이 나오면 그 자리에서 문서에 적는다.**
   질문에 답하다 정해진 것, 리뷰 코멘트로 합의된 것 모두 해당한다.
3. **코드에는 인용한 SPEC 절을 주석으로 남긴다** — `/** ... (SPEC §5.1) */`.
4. **왜 바꿨는지를 함께 적는다.** 값만 고치면 다음 사람이 "잘못 옮겼나" 하고 되돌린다.

CI 의 **Spec sync check** 가 바뀐 코드에서 `SPEC §x.y` 참조를 뽑아 확인할 절 목록을 띄운다.
막지는 않는다. 판단은 사람이 한다.

> ⚠️ **S8 러닝코스 API 계약 한 건은 아직 문서 간 불일치가 남아 있다.**
> `screen-api-matrix.md` v1.0은 08-17 화면 플로우와 상태를 반영했지만, S8은 기존 API 계약대로
> `/courses/near`와 `/walk-spots`를 따로 호출하는 형태를 유지한다. 최신 `SPEC.md` §4.11과
> 포팅 계획서는 서버가 두 소스를 합쳐 한 목록으로 반환하는 방향이다.
>
> **S8 러닝코스 관련 API 를 만질 때는 매트릭스를 그대로 믿지 말고 `SPEC.md` §4.11 과 대조한다.**
> 알려진 불일치(`/walk-spots`)는 이슈 #19와 참고 문서 7장. 정리되면 이 경고를 지운다.

### 에이전트에게 시킬 때

"이 기능 만들어" 라고 한 번에 시키지 않는다. **계약 작성과 구현을 나눠서 시킨다.**
연결부가 걸린 작업을 받으면 에이전트는 구현 전에 계약이 문서에 있는지 확인하고,
없으면 **먼저 계약 초안을 제안**한다. 구현 중 명세와 다른 선택을 하게 되면 **말없이 진행하지 않는다.**

---

## 6. ⚠️ 원본을 그대로 옮기면 틀리는 자리

`reference-web/` 은 SPEC 보다 낡았다. 근거는 `docs/domain-logic-audit.md`.

| 항목 | 하지 말 것 → 할 것 |
|---|---|
| 산책 블록 3개 | 원본에 있지만 **빼고** 옮긴다 (§5.6) |
| `rule.walk` | 거리 라벨 아님. **S7→S8 목표거리 `min(walk,5)km`** |
| 대회 블록 | `RACE` + `systemManaged`. 편집 연산 **전부 거부** (서버는 409) |
| `courseRegions()` | 시도명만 말고 **개수도** 반환 (칩에 숫자 표시) |
| 걷기 스팟 limit | 원본 10 → **12** |
| **두루누비 동기화** | **API 응답으로 덮어쓰지 말 것.** 261 → 144 로 반토막. `API_GPX`+`GPX_ONLY` 둘 다 서비스 (§8.4) |

서울에는 두루누비 코스가 사실상 없다(반경 8km 내 0건). 걷기 스팟은 폴백이 아니라 **수도권의 기본 경험**이다.

---

## 7. 변경 권한과 금지

- **요청받지 않은 대규모 리팩터링을 하지 않는다.** 개선점은 **말하고**, 실행은 별도 작업으로.
- **새 라이브러리 · 공개 API · DB 스키마 변경은 먼저 묻는다.**
- **남의 담당 영역은 먼저 묻는다.** 급하면 최소 변경만 하고 PR 에 명시한다.
- **사용자가 한 변경을 임의로 되돌리지 않는다.** 이유가 보이면 말하고 판단을 받는다.
- **커밋 · push · PR 생성 · 머지는 요청받았을 때만.** 파일 수정까지가 기본 범위다.
- **테스트를 지우거나 `@Ignore` 로 막아서 통과시키지 않는다.** 깨지면 원인을 보고한다.

---

## 8. 보안

**절대 커밋하지 않는 것** — `local.properties` · `.env` · `*.jks` · `*.keystore` · API 키 ·
`build/` · `.gradle/` · `.idea/`

| 무엇 | 어디 두나 |
|---|---|
| 카카오 **네이티브** 키 (앱용) | `local.properties` → `BuildConfig` 주입 |
| KTO · 카카오 **REST** 키 | **서버에만.** 앱은 뜯으면 키가 나온다 |
| 데이터 스크립트용 키 | `scripts/.env` (gitignore) |
| 서버 DB · 시크릿 | 서버 환경변수 |

- 키가 필요하면 `.env.example` 에 **빈 값으로 항목만** 추가한다.
- **로그에 남기지 않는 것**: 토큰 · 비밀번호 · 인증 코드 · 이메일 주소 · 사용자 좌표.
- **키가 노출되면**: ① 해당 콘솔에서 **즉시 폐기 · 재발급** ② 팀에 알린다
  ③ 커밋에서 지워도 git 이력에 남으므로 **되돌리기로 끝내지 않는다.**

---

## 9. Git · PR

**Git Flow.** 기본 브랜치는 `develop`.

```bash
git switch develop && git pull
git switch -c feature/작업이름      # develop 에서 딴다
git push -u origin feature/작업이름
# → develop 대상 PR, 최소 1명 승인 후 Squash and merge
```

- `main` 은 릴리스 때만. 평소 PR 대상은 항상 `develop`.
- **브랜치 접두사와 커밋 종류가 한 글자 다르다** — 브랜치 `feature/`, 커밋 `feat`.
  나머지(`fix` `docs` `data` `chore` `refactor` `style` `test`)는 양쪽이 같다.
- 커밋 메시지 `<종류>(<범위>): <설명>` — 한국어, 마침표 없이.
- **PR 하나에 기능 하나.** 리뷰할 수 없을 만큼 크면 나눈다.
- PR 본문은 템플릿을 채운다 — 작업 내용 · 확인 방법 · **명세 반영** · 참고.
- 머지된 브랜치는 삭제한다.

---

## 10. 완료 조건과 보고

구현만 끝난 것은 완료가 아니다.

- [ ] 구현이 요청 범위를 **전부** 덮는가 (일부만 하고 끝냈다고 하지 않는다)
- [ ] 3장의 **최소 검증**을 실제로 돌렸는가
- [ ] `domain/` 을 건드렸다면 테스트가 있는가
- [ ] 명세와 달라진 것을 **같은 PR 에서** 반영했는가 (없으면 "없음" 이라고 밝힌다)
- [ ] UI 를 바꿨다면 **스크린샷**을 붙였는가

**돌려보지 못한 것을 "통과했다" 고 말하지 않는다.** 환경이 없어 못 돌렸으면 그렇게 적는다.

### 보고 형식

```
무엇을 바꿨나    파일과 변경 요지
왜              어떤 요구·근거에서 나왔는지 (SPEC 절 · 결정 번호)
어떻게 검증했나   실제로 돌린 명령과 결과
검증 못 한 것    환경 없음 · 시간 부족 등 이유와 함께
결정이 필요한 것  팀원이 판단해야 할 것, 내가 세운 가정
```

실패한 것을 성공으로 보고하지 않는다. 테스트가 깨졌으면 출력과 함께 그대로 알린다.

---

## 11. 역할 분담

| 팀원 | 영역 | 백로그 |
|---|---|---|
| 유선경 | 백엔드 (Spring Boot + PostgreSQL) | AP-02 · 07 · 23 · 24(서버) |
| 이건모 | 앱 코어 — domain/data · 지도 · GPX · GPS · CI | AP-01 · 03 · 04 · 05 · 12 · 14 · 22 · 24(앱) |
| 김민지 | 앱 UI — Compose · 내비 · 화면 | AP-06 · 08 · 09 · 10 · 11 · 13 · 21 |

`ui/` ↔ `domain/` 경계는 `UiState` 를 합의하고 병렬로 간다(4장).
