# ── secrets (커밋 금지) ──
local.properties
.env
.env.local
*.jks
*.keystore

# ── Android / Gradle ──
.gradle/
build/
captures/
.externalNativeBuild/
.cxx/
*.iml
.idea/
*.apk
*.aab

# ── Python (scripts/) ──
__pycache__/
*.pyc
geocode_cache.json
output/

# ── Node (reference-web/) ──
node_modules/
dist/

# ── OS / editor ──
.DS_Store
*.log
*.tmp

# ── agent workspaces ──
# .claude/ 는 팀 공유한다(settings.json·skills·commands). 개인 설정만 제외.
.claude/settings.local.json
.claude/local/
.codex-orchestrator/

# Local-only documentation
/docs/local/
