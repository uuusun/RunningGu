# ── secrets (커밋 금지) ──
local.properties
.env
.env.local
/backend/deploy/env/*.env
*.jks
*.keystore

# ── Android / Gradle ──
.gradle/
build/
# VS Code · Eclipse 의 Java 빌드 출력 자리다. Gradle 은 build/ 를 쓰지만
# 에디터가 열면 bin/ 에 .class 와 소스 복사본을 또 만든다 (#109).
bin/
captures/
.externalNativeBuild/
.cxx/
*.iml
.idea/
*.apk
*.aab

# ── Python (scripts/) ──
__pycache__/
*.pyc
geocode_cache.json
output/

# ── 러닝코스 원천 데이터 (커밋 금지 — 공공데이터 배포본 200MB+) ──
.cache/
data/100대명산/
data/한국등산트레킹지원센터_*/
data/courses_gpx/

# ── Node (reference-web/) ──
node_modules/
dist/

# ── OS / editor ──
.DS_Store
*.log
*.tmp

# ── agent workspaces ──
# .claude/ 는 팀 공유한다(settings.json·skills·commands). 개인 설정만 제외.
.claude/settings.local.json
.claude/local/
.codex-orchestrator/

# Local-only documentation
/docs/local/
