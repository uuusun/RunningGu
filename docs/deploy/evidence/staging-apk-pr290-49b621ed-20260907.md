# #290 검증용 staging 서명 APK — 2026-09-07

## 빌드 식별

| 항목 | 값 |
|---|---|
| 용도 | #287/#290 마케팅 동의 복원 기기 검증. 최종 스토어 제출본 아님 |
| 앱 소스 | `49b621ed9111f6e55534c50e7b3d50cd9c3485bd` (#290 head 고정) |
| 작업 디렉터리 | `.codex-worktrees/release-pr290-20260907` (detached HEAD, 소스 수정 없음) |
| APK | `.cache/release-artifacts/20260907-pr290-49b621ed/runninggu-staging-pr290-49b621ed-newkey.apk` |
| 크기 | `44,721,194` bytes |
| 파일 SHA-256 | `8730B371DC39F0C4D2C92DC153C898617633E1D90AF2B7F78FDA05F8AF5000B5` |
| applicationId | `com.runninggu.app` |
| versionCode / versionName | `1` / `1.0` — 격리 시험용 빌드의 기존 값 |
| API 주소 | `https://staging-api.runninggu.store/api/` |
| 인증서 SHA-256 | `41073757d098062b234c83695c04bd4753640fe8da86bb1f2ba3a2b2fb734fc7` |
| 카카오 키 해시 | `oRJtsESChTHqkP9fnp4lRq+wNxM=` — 09-05 새 출시 키와 동일 |
| 서버 커밋 | **미확인.** 4GiB 측정 중이므로 이번 작업에서 조회·변경하지 않음 |

## 실제 검증

- JDK 21에서 `gradlew.bat :app:testDebugUnitTest :app:assembleRelease --offline --no-daemon --console=plain` 성공(3분 57초). **108개 테스트 클래스, @Test 881개, 실패·오류·건너뜀 0**.
- Gradle에서 unsigned release를 만든 뒤 SDK build-tools 36.0.0 `zipalign -c -P 16 4` 검사, 선경의 기존 출시 키로 `apksigner sign`, `apksigner verify --verbose --print-certs` 성공. R8·리소스 축소가 적용된 산출물이다.
- 비밀번호는 기존 CurrentUser DPAPI 보관본을 프로세스 환경변수로만 복원해 사용하고 제거했다. 키 파일을 복사·업로드하지 않았고 local.properties에 서명 비밀번호를 기록하지 않았다.
- APK 내부 DEX에서 staging 주소·네이티브 키 주입 여부를 값 노출 없이 확인했다. `api.runninggu.example`은 없다.
- APK 권한: `INTERNET`, `ACCESS_NETWORK_STATE`, `DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION`. 위치 권한 없음.
- 빌드 로그 `.cache/pr290-build-20260907.log`, 테스트 집계 `.cache/pr290-test-summary-20260907.json`, 서명 검증 `.cache/pr290-signing-verification-20260907.log`.

## 건모에게 넘길 시험 항목

**부하 시험 종료와 시험 서버 개방 확인 후** 전용 시험 계정으로 수행한다. 표본 계정의 동의를 임의로 변경하지 않는다. 이전 [375b6077 APK](staging-apk-375b6077-20260905.md)의 기기 결과를 이 빌드의 결과로 재사용하지 않는다.

| 시나리오 | 확인할 결과 | 기록 |
|---|---|---|
| 마케팅 동의 ON → 로그아웃 → 재로그인 | 서버의 ON이 복원됨 | 미실행 |
| 마케팅 동의 OFF → 로그아웃 → 재로그인 | 서버의 OFF가 복원됨 | 미실행 |
| 설정 최초 조회 지연 | 값을 모르는데 OFF로 표시하지 않고 로딩·입력 상태를 올바르게 표시 | 미실행 |
| 설정 조회 실패 → 재시도 | 실패를 OFF로 오인하지 않으며 재시도로 실제 값 복원 | 미실행 |
| 동의 변경 실패 | 성공한 것처럼 값이 남지 않고 오류·재시도/복구 동작 확인 | 미실행 |
| 새 출시 인증서 | 카카오 로그인 이후 서버 세션 발급 완료, 지도 렌더링 | 미실행 |

기기·Android/API·내비 방식, 앱 SHA와 서버 SHA, APK SHA-256, 각 시나리오의 전/후 관찰을 글로 남긴다. PR에 스크린샷을 첨부하지 않는다.

**완료된 것은 로컬 APK 제작과 전달 자료 준비다.** 팀원의 APK 파일 수신·설치, 실제 서버 통신·기기 시험, #290 승인, 원스토어 업로드는 완료하지 않았다. 최종 운영 APK는 후속 변경을 통합한 소스와 운영 주소로 별도 제작한다.
