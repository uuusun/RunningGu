# 약관·동의 문안

A2 회원가입에서 사용자에게 보여주는 문안이다. 서버가 `USER_AGREEMENT` 에 저장하는
**버전과 여기 문안이 1:1로 대응해야 한다** — 서버는 `PRIVACY 1.2` 를 저장하는데 앱이
보여준 게 다른 글이면 동의 이력이 무엇에 대한 동의인지 알 수 없다(NFR-12).

> ## 외부 법률 검토 상태
>
> 세 공개 문안은 제품의 실제 수집·처리 흐름과 내부 대조했지만 **외부 법률 검토를 완료하지
> 않았다.** 2026-09-09 팀 결정으로 외부 검토는 최초 출시 차단 조건에서 제외했다. 아래 항목은
> 이후 전문가 검토를 받으면 다시 확인할 법률 쟁점이며, 완료했다고 표시하지 않는다.
>
> - 개인정보 수집·이용 동의의 법정 고지 항목 충족 여부 (개인정보 보호법 §15·§22)
> - 보유·이용 기간과 파기 절차의 적정성
> - 만 14세 이상만 가입받고 별도 필수 확인을 두는 정책의 적정성. 생년월일·실제 나이는
>   수집하지 않으며, 화면·API에는 별도 필수 확인을 구현했다(#228)
> - 마케팅 수신 동의의 표시 의무. P0에서는 마케팅 메일을 보내지 않으며, 실제 발송 범위를
>   열 때 수신거부 링크와 `MARKETING 1.1`을 함께 검토한다(#229)
> - AWS 실제 계약 법인명(Seller of Record)과 Gmail 문의 처리 완료 후 1년 보유·Google LLC
>   국외 처리 문안의 적정성. Google의 전 세계 서버 안내를 이전 국가로 어떻게 표시할지도
>   확인하고, 브랜드나 추정 법인명을 최종 문안에 임의로 쓰지 않는다(#230)
> - 암호화 일일 백업 7일 보관과 복원 시 탈퇴 데이터 재삭제 절차의 적정성(#227)
> - **위치정보법** 검토 — 러닝코스는 **이용자가 검색하거나 눌러서 고른 장소**의 좌표를
>   서버로 보내 결과를 받는다. **기기·네트워크 신호로 현재 위치를 자동 추정하는 경로는
>   전부 없앴다**(#215 — GPS · Wi-Fi AP · Cell-ID · BLE · IP 지오로케이션). 고른 장소의
>   좌표도 위치기반 서비스로 볼지 확인해야 한다 (#133 리뷰 · #111)
> - **남용 방지 목적의 IP 처리** — 중복 확인 rate limit 이 IP 를 서버 메모리에 두고,
>   마지막 사용 후 2분에 만료되며 이후 캐시 유지보수 시 제거된다. 별도 동의가 필요한지,
>   지금 문안으로 고지가 충분한지 (#133 리뷰)

## 파일

| 파일 | `AgreementType` | 필수 | 출시 소스 상태 |
|---|---|---|---|
| [`v1.1/tos.md`](v1.1/tos.md) | `TOS` | ✅ | ✅ `1.1` |
| [`v1.2/privacy.md`](v1.2/privacy.md) | `PRIVACY` | ✅ | ✅ `1.2` |
| [`v1.1/marketing.md`](v1.1/marketing.md) | `MARKETING` | 선택 | ✅ `1.1` |
| [`v1.0/tos.md`](v1.0/tos.md) | `TOS` | ✅ | 🕓 지난 버전 — 이 번호로 받은 동의가 있다 |
| [`v1.0/privacy.md`](v1.0/privacy.md) | `PRIVACY` | ✅ | 🕓 지난 버전 — 이 번호로 받은 동의가 있다 |
| [`v1.0/marketing.md`](v1.0/marketing.md) | `MARKETING` | 선택 | 🕓 지난 버전 — 이 번호로 받은 동의가 있다 |
| [`v1.1/privacy.md`](v1.1/privacy.md) | `PRIVACY` | ✅ | ❌ **켜지 않았다** — v1.2 로 대체 (#215) |

**셋의 번호가 서로 다르다.** PRIVACY 만 `1.1` 을 건너뛰었다 — 아래 "버전 규칙" 을 보라.

## 버전 규칙

- 디렉터리 이름이 곧 버전이다. `v1.2/privacy.md` ↔ 서버 `privacy-version: "1.2"`
- **한 번이라도 활성이었던 버전의 문안을 고치면 버전을 올린다.** 오타 수정도 마찬가지다 —
  같은 버전에 다른 글이 있으면 이력이 가리키는 대상이 흔들린다
- **이미 나간 버전의 파일은 고치지 않는다.** 서버가 그 번호로 저장한 동의 이력이
  가리키는 글이라, 나중에 손대면 "무엇에 동의했는가" 가 달라진다
- **아직 켜지 않은 버전은 제자리에서 고친다.** 위 표에서 `⏸` · `❌` 인 것들이다. 그 번호로
  저장된 동의 이력이 **하나도 없어서** 가리키는 대상이 흔들리지 않는다. 검토받는 동안
  번호만 올라가면 "무엇이 최신 초안인가" 가 오히려 흐려진다
  (2026-09-06 · `v1.2/privacy.md` 사실관계 정정에서 이 경우가 처음 나왔다)

### 문안을 쓰는 것과 그 버전을 켜는 것은 다른 일이다

새 버전 디렉터리를 **추가하는 PR** 과 서버의 활성 버전을 **올리는 PR** 은 나눈다. 문안이
먼저 있어야 검토할 수 있는데, 검토가 끝나기 전에 서버만 올리면 **앱은 옛 글을 보여주고
서버는 새 버전으로 이력을 남긴다.**

기기 위치 제거(#215), 가입 연령 확인(#263·#264), 인증·세션 정리(#240)가 `develop`에
반영되어 세 공개 문안과 현재 제품 범위가 맞는다. #278은 출시 소스값을
`TOS 1.1`·`PRIVACY 1.2`·`MARKETING 1.1`로 맞춘다. 이 소스 변경만으로 운영 전환을 완료한
것은 아니며, 아래 단계별 확인 뒤 운영 서버와 공개 앱에 적용한다.

활성 버전을 올릴 때는 아래 넷을 **같은 PR 에서** 맞춘다.

1. 서버 `runninggu.auth.agreements.{type}-version`
2. 앱 `AgreementDoc.{TYPE}.version` — **약관마다 따로 든다.** 서버도 셋을 각각 관리한다.
3. 그 버전 폴더에 **해당 파일이 실제로 있는지** — `AgreementTextsTest` 가 막는다
4. 위 표의 "서버 활성" 열

### ⚠️ · 🔄 블록은 켜기 전에 걷는다

문안 안의 `⚠️ 미결` · `🔄 리뷰 중` 블록과 **이슈·PR 번호**는 팀이 읽는 초안 표시다.
사용자에게 그대로 나가면 **약관에 내부 진행 상황이 실린다.** 활성 버전을 올리는 PR 에서
지우거나 확정 문장으로 바꾼다.

지울 수 없으면 **아직 켤 때가 아니라는 뜻**이다 — 미결이 남았다는 말이니까(#217 리뷰).

> **`PRIVACY 1.1`은 한 번도 켜지 않은 채로 끝났다.** 검토가 끝나기 전에 기기 위치 기능이
> 없어져서(#215) 문안이 사실과 어긋났다. **이미 나간 버전이 아니므로 고치지 않고 새
> 버전을 뒀다** — 위 "버전 규칙" 을 그대로 따른 것이고, 서버가 `1.1` 로 저장한 동의
> 이력은 하나도 없다. 파일은 무엇이 왜 달라졌는지 대조할 수 있게 남긴다.

## 어디에 두고 어떻게 보여줄 것인가 — **이 폴더를 앱에 번들한다** (#191)

A2 동의 단계의 각 줄에 [보기] 가 있고, 누르면 전문이 뜬다. 앱은 **이 폴더의 파일을 그대로**
읽는다 — `android/app/build.gradle.kts` 가 여기를 assets 소스로 물려 둔다.

```kotlin
assets.srcDir(rootProject.file("../docs/agreements"))
```

`docs/agreements/v1.0/tos.md` → assets 의 `v1.0/tos.md`. **사본이 없으므로 드리프트할 자리가
없다.** 앱이 보여준 글과 서버가 저장한 버전이 어긋나면 동의 이력이 무엇에 대한 것인지 알 수
없다(NFR-12)는 것이 이 방식을 고른 이유다.

- 읽는 쪽은 `data/local/AgreementTexts` · `AgreementDoc`
- 마크다운 **기호만** 걷어 낸다(`AgreementMarkdown`). 문장은 안 바꾼다
- `README.md`(이 파일)는 팀 내부 문서라 `ignoreAssetsPatterns` 로 APK 에서 뺀다

### 서버가 본문을 주게 되면

아래 A 안이 여전히 낫다. **그때 바뀌는 것은 `AgreementTexts.load` 하나**이고 화면은 그대로다.

| 안 | 방식 | 평가 |
|---|---|---|
| **A** | 서버가 `GET /api/agreements/{type}` 로 본문 제공 | 버전과 문안이 한 곳. 앱 배포 없이 개정 가능 |
| B | 서버가 웹 페이지로 서빙 + Custom Tabs | 서버가 이미 재설정 페이지를 서빙(§4.3). `androidx.browser` 새 의존성 |
| ~~C~~ | ~~앱에 문자열로 박기~~ | 버전과 문안이 갈라진다. **번들은 이것과 다르다** — 원본 파일을 그대로 싣는다 |

지금 방식은 앱 배포 없이 문안을 고칠 수 없다는 점에서 A 보다 약하다. 다만 **P0 에서는 문안을
고치면 어차피 서버 활성 버전도 함께 올려야 하므로**(아래 절차) 앱 배포가 한 번 더 드는 것이
전부다.

## 확정한 계약과 활성화 조건

2026-08-31에 #227~#230에서 확정한 계약과 2026-09-11 현재 검증 상태다. 소스 정합, 운영
배포, 앱 공개를 서로 다른 단계로 기록한다.

| 항목 | 확정값 | 활성화 전에 필요한 것 |
|---|---|---|
| 가입 연령 | 만 14세 이상만 가입. 전체 동의 밖의 별도 필수 확인, `ageOver14` 요청 필드, 생년월일·별도 연령 컬럼 없음 | #263 서버와 #264 앱 구현·계약 테스트 완료 |
| 인증·세션 보유 | 요청 시각 기준 만료. 성공한 인증 기록 즉시 삭제, 만료 기록 시작 시·매시간 정리(정상 1시간·최대 24시간), Refresh 14일과 재사용 탐지 | #240 구현·마이그레이션 완료. 탈퇴 데이터 복구 리허설은 아래 체크리스트로 관리 |
| 인프라 예산·메모리 | staging·production 모두 `c7i-flex.large` 4GiB. production 월 USD 70, 실제 80%·예상 100% 알림 | 운영 배포·예산 알림·4GiB 재부팅 스모크 완료(#336) |
| 운영 주체·문의 | 운영 주체 `루트메이트`, 담당 부서 `루트메이트 개인정보보호 담당`, `runninggu.play@gmail.com`. 문의 처리 완료 후 1년 보관 뒤 삭제하고 Google LLC 국외 처리를 고지. `privacy@runninggu.store`·수신 MX는 구성하지 않음 | 실제 Gmail 운영 사실과 문안 내부 대조 완료 |
| 메일 | Resend로 거래성 메일만 발송, 발신 `no-reply@runninggu.store`. P0 마케팅 메일·관심 지역 추정 발송 없음 | 도메인 검증, staging·production 환경 구성, 가입·재설정 실제 받은편지함 E2E 완료(#321·#338) |
| 백업 | pgBackRest 일일 전체 백업 + 연속 WAL을 서울 리전 KMS 암호화 비공개 S3에 7일 보관, 운영책임자만 접근. 복원 후 탈퇴 삭제를 재반영·검증하기 전 비공개 | 이미지·Compose·timer·실패 알림, 빈 DB 복원, 격리된 탈퇴 전후 WAL 복구 리허설 완료 |

### 활성화를 막는 최종 확인

- [x] 백엔드 예외 원문 제거와 nginx 최소 접속 로그를 운영 환경에 적용하고, 가짜 이메일·
      좌표·토큰 표식 및 최근 보존 로그의 일반 민감정보 패턴이 nginx 파일·backend/nginx
      journal에서 0건인지 확인했다
      - staging은 과거 로그 86건 정리와 `a2c7700` 배포 뒤 전체 0건·`passed=true`, production은
        안전 release 전 archive 1건 정리 뒤 nginx 4개·216행과 최근 14일 journal 211행의
        시험 표식·일반 패턴 0건·`passed=true`를 확인했다.
        [실서버 적용·검증 기록](../deploy/evidence/server-log-privacy-staging-production-20260910.md)
- [x] #227의 인증·세션 정리와 탈퇴 데이터 복구 절차가 구현·검증됐다
      - 합성 사용자 생성·백업·탈퇴 뒤 즉시 복원에서는 사용자가 되살아나는 것을 확인하고,
        최신 WAL까지 적용한 복원에서는 사용자·동의·저장 코스가 모두 0건임을 확인했다.
        운영 DB와 운영 볼륨은 리허설 전후 그대로였다.
        [탈퇴 데이터 WAL 복구 리허설](../deploy/evidence/withdrawal-recovery-drill-20260911.md)
- [x] #228의 A2 별도 체크박스, 두 가입 API `ageOver14` 검증, 가입 `CODE_EXPIRED`의
      `mustResend=true`·재발송 안내가 구현·검증됐다
      - 서버 #263과 앱 #264가 `ageOver14` 계약을 구현했고, Android `AgeOver14Test`와
        `SignupFailureMessageTest`, 백엔드 가입 통합 테스트가 각 오류 분기를 고정한다.
- [x] 서버 소스 활성 버전을 `TOS=1.1`, `PRIVACY=1.2`, `MARKETING=1.1`로 바꾸고 앱 문안
      버전과 같은 변경에서 맞췄다
      - 서버 `application.yml` 세 값과 앱 `AgreementDoc` 세 값을 같은 PR(#278)에서 바꿨다.
        `ApplicationContextTest` 와 `AgreementTextsTest` 가 양쪽 값을 각각 못으로 박는다.
      - 이 체크는 소스 정합만 뜻한다. 운영 서버 배포와 앱 공개 여부는 아래 단계에서 관리한다.
- [x] AWS 호스팅 계약 법인명과 Resend 법인·미국 처리·보유 사실을 각 서비스의 실제 계정·공식 문서와 내부 대조했다
      - AWS Billing의 실제 계정 `Service provider`가 `Amazon Web Services Korea LLC`임을
        확인했다. Resend DPA의 `Plus Five Five, Inc.`·주소·연락처, 미국 메타데이터 저장,
        이메일 데이터 30일 보유를 공식 문서와 대조했다.
- [x] 외부 법률 검토를 완료했다고 표시하지 않고, 공개 문안에서 내부 `⚠️`·`🔄`·이슈 번호·
      초안 안내를 제거했다
      - 외부 법률 검토 미완료 사실과 최초 출시 비차단 결정을 이 README에만 남긴다.

### 소스 정합 뒤 운영 활성화 순서

- [x] 앱·서버 출시 소스값과 세 문안 파일을 같은 변경에서 맞추고 자동 테스트로 고정
- [ ] 최종 서명 APK의 A2에서 이용약관 1.1, 개인정보 1.2, 마케팅 1.1 전문을 각각 확인
- [ ] 스토어 심사 완료 뒤 운영 서버를 같은 약관 버전 소스로 전환
- [ ] 운영 서버 전환 확인 뒤 앱 공개
- [ ] 공개 앱으로 신규 가입하고 `USER_AGREEMENT`의 세 버전과 같은 `changedAt`을 확인

P0에서 마케팅 메일을 보내지 않으므로 수신거부 링크는 이번 활성화의 차단 항목이 아니다.
향후 발송 기능을 열 때 공개 수신거부 API·토큰·페이지와 `MARKETING 1.1`을 같은 계약으로
먼저 확정한다(#229).
